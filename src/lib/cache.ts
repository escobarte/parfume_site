/**
 * Страховочный TTL для кэша витрины.
 *
 * Обычный путь инвалидации — теги: правка в /admin идёт внутри Next и хук
 * коллекции зовёт revalidateTag сразу. Но `pnpm seed` и CSV-импорт работают
 * отдельным процессом и до кэша живого сервера не достают — без TTL их
 * изменения были бы видны только после передеплоя.
 */
export const CACHE_TTL = 300
