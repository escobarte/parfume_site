# STATE.md — где мы сейчас

> **Обновлён:** 2026-09-04, сессия чат-штаба — **фаза 9.2 закрыта**: режим «Сайт в разработке». Клиент начал анонс в соцсетях (Instagram, TikTok обещан позже), сайт закрыт от посторонних одной переменной окружения (`MAINTENANCE_MODE=1`), владелец и клиент сохраняют полный доступ по секретной ссылке с байпасом через cookie (30 дней). Заглушка — в стиле главной (тот же `BrandMark`, тот же `styles.css`, без новых токенов), контакты и соцсети кликабельны, поиск/корзина/кнопка CATALOG на заглушке не рендерятся. `/admin` и `/api` остаются доступны всегда. Отдельной правкой в тот же день — телефон на заглушке заменён на актуальный (+373 78 201 802), физический адрес с заглушки убран по просьбе клиента (адрес остаётся на странице «Контакты» и в JSON-LD — там не трогали). Регресс не задет (правки изолированы в `maintenance/`), `tsc`/`lint`/`pnpm build` чистые. **Прод сейчас закрыт заглушкой для всех, кроме владельца/клиента с байпасом** — учитывать это в начале следующей сессии. Ручная приёмка (Приложение A) теперь доступна в любой момент через секретную ссылку, не нужно ждать отдельного окна. **Следующий шаг не изменился по сути:** (1) GA4, (2) `backup-media.sh` на VPS + cron + Postgres-бэкапы в Coolify, затем ручная приёмка → фаза 10 → снять `MAINTENANCE_MODE` перед реальным запуском.
>
> Единственная точка передачи между сессиями. Читают все: Claude Code (при старте сессии), чат-штаб на claude.ai (файл приложен в файлы проекта), владелец.
>
> **Правило записи: этот файл обновляет только владелец (руками или через чат-штаб).** Claude Code его читает, но никогда не редактирует и не создаёт.
>
> Здесь — только состояние. Правила работы → `CLAUDE.md` · спека → `docs/PLAN.md` · промпты → `docs/PROMPTS.md` · грабли → `docs/GOTCHAS.md` · история → `docs/CHANGELOG.md`. Ничего из этого сюда не копировать.

## Проект в двух строках

Сайт-каталог парфюмерии **MON FLACON** для рынка **Молдовы**. Витрина без онлайн-оплаты: карточки товаров, фильтры, корзина, форма заявки. ~100+ товаров × 5–7 объёмов → модель `Product → Variants` (~600 SKU). Локали **RO / RU / EN**, валюта **MDL**. Сборку делает **Claude Code** (терминал WSL), чат claude.ai — штаб: планирует, разбирает ошибки, готовит документы клиенту. Проект локально: `~/projects/parfume_site`.

## Где мы

**Закрыто:** фазы 0 → 4.7.x включительно, багфикс UX 4.x (вспышка пустой корзины на пути «Отправить заявку → Спасибо»), добор к фазе 5 (точка входа в отслеживание заказа из UI), **фаза 5** (главная из CMS по вайрфреймам/мокапу, коллекция Pages со статикой О нас/Доставка/Возврат/Контакты, `/brands` + страницы брендов, select→relationship на промо-ссылках и меню шапки с миграцией старых значений, кастомные 404/error на 3 локалях, ревизия переводов + `docs/translations-review.md`), **фаза 7** (SEO + производительность) и **фаза 8** (тестирование — см. ниже). Каталог, корзина, заявки, акции, поток заявки, отслеживание заказа, админка, контентные страницы, SEO-разметка и тестовое покрытие — работают. Детали каждой фазы — `docs/CHANGELOG.md` и архив в `docs/archive/`.

**Фаза 7 — что именно закрыто:** `generateMetadata` (canonical/hreflang/OG) на всех страницах, `noindex` на отфильтрованных выдачах, генерация OG-картинок, JSON-LD (Product+AggregateOffer/BreadcrumbList/LocalBusiness), `sitemap.xml`/`robots.txt` из БД, GA4 (4 события) за cookie-баннером согласия. Lighthouse (Chromium из WSL, прод-сборка, mobile) — **perf 96/96/96, a11y 96/95/96, best-practices и SEO 100/100/100** на главной/категории/товаре, отчёты в `docs/perf/`. Один пункт **осознанно не тронут**: контраст бренд-строки на главной (`#9AA6B4` на белом) не проходит WCAG AA — но это буквальный хекс из `docs/WIREFRAMES.md` §5, не баг реализации; менять зафиксированный дизайн — решение владельца, не Claude Code.

**Фаза 8 — что именно закрыто:** 4 формальных E2E-сценария на `@playwright/test` (`tests/e2e/`) — фильтры с проверкой URL-состояния, переключение объёма с изменением цены, корзина с количеством и `reload`, сквозная заявка при `ORDERS_DRY_RUN=1` с проверкой записи в БД — плюс smoke на 3 типах страниц × 3 локали. Три прогона `pnpm test:e2e` подряд — 19/19 без единого флака (после фикса: параллельные воркеры `@playwright/test` создавали гонку в клиентской навигации на общем dev-сервере — исправлено `workers: 1` в `playwright.config.ts`, задокументировано в `docs/GOTCHAS.md`). Нагрузочный smoke — 120 синтетических товаров × 6 вариантов (`pnpm seed:load`, скрипт `scripts/seed-load.ts`) поверх демо-каталога, отклик листинга/фильтров/поиска/админки на 132 товарах/755 SKU — 5–312мс, без деградации (`docs/perf/full-load.md`); синтетические данные удалены после замера (`pnpm seed:load:clean`), каталог возвращён к 12 демо-товарам. **Ручная проверка (Приложение A, ★-пункты) НЕ пройдена** — только автоматика; сокращённая шпаргалка из 10 ★-пунктов передана владельцу в чате этой сессии, физически пройти её с телефона нужно после прода (см. «Ждёт владельца» — там же два пункта, которые в деве принципиально не проверить: боевая доставка в Telegram и https без mixed content).

**Фаза 9.2 (режим «Сайт в разработке») — закрыта 2026-09-04.** Причина: клиент начинает анонс в соцсетях раньше готовности сайта. Реализация — флаг в env + логика встроена в существующий `src/proxy.ts` перед `createMiddleware` next-intl (второго мидлвари не заводилось) + байпас по `?preview=<токен>` с timing-safe сравнением (SHA-256 через `crypto.subtle` — `node:crypto` недоступен в edge-рантайме) и cookie на 30 дней. Исключения из заглушки: `/admin`, `/api`, `/_next`, `/media`, `robots.txt`, `sitemap.xml`, генератор og-превью — последнее критично, иначе `facebookexternalhit` не соберёт превью для соцсетей. `robots.txt` сознательно не тронут по той же причине. Заглушка — в стиле главной страницы (`src/app/(maintenance)/maintenance/[locale]/`, отдельная группа маршрутов со своим layout — витринный не переиспользован, он тянет глобалы из Payload, а заглушка обязана отвечать даже при недоступной БД), три локали, кликабельные контакты (`tel:`, `mailto:`, Google Maps — адрес позже убран отдельной правкой), кнопки соцсетей рендерятся только при заполненной переменной (сейчас: Instagram есть, TikTok — пусто, клиент даст ссылку позже, тогда нужен полный Redeploy, не рестарт — `NEXT_PUBLIC_*` подставляются на сборке). В тот же день отдельной сессией — телефон на заглушке заменён на актуальный, физический адрес убран по просьбе клиента (только с заглушки, страница «Контакты» и JSON-LD не тронуты). Полные детали — `docs/CHANGELOG.md`, записи `[2026-09-04] Фаза 9.2`.

**Два блокера деплоя фазы 9, найдены и закрыты 2026-08-18 — оба всплыли при реальной, не тестовой попытке выкатить контейнер.**

1. **Билд-блокер.** `pnpm build` в Docker падал на пререндере `/sitemap.xml` (`relation "brands" does not exist` — на этапе сборки образа БД ещё недоступна/не смигрирована, Coolify собирает образ раньше, чем стартует Postgres) и ронял всю сборку целиком. Причина — `src/app/(frontend)/sitemap.ts` (фаза 7.3) единственный не получил защиту `staticParamsOrEmpty`, которой уже обёрнуты все остальные маршруты с запросами к БД на сборке. Исправлено тем же хелпером (не новый), проверено вживую: `docker compose -f docker-compose.dev.yml stop postgres` → `pnpm build` (было падение → стало `exit 0`, sitemap = 9 статических URL без БД) → `start postgres` → повторный `pnpm build` (`exit 0`, sitemap = полные 78 URL).
2. **Деплой-блокер (проявился уже ПОСЛЕ первого билд-фикса, на живом контейнере в Coolify).** Контейнер поднимался с пустой БД — `relation "homepage" does not exist» — Payload схему сам не создаёт, а `output: 'standalone'`-сборка не тащит в финальный образ ни CLI `payload`, ни исходники `src/` (`payload migrate` внутри контейнера падал `MODULE_NOT_FOUND`). Добавлен `docker-entrypoint.sh`: перед `node server.js` сам прогоняет `payload migrate`. Проверено докером «по-настоящему» — чистый `postgres:17-alpine` без единой таблицы в изолированной сети, контейнер поднят против него: 9 миграций накатились сами, `homepage`/`brands`/... появились, HTTP 200; второй и третий старт того же контейнера на уже смигрированной БД — идемпотентно, без повторного накатывания и без ошибок (не предполагалось, а подтверждено трижды). Сид (`pnpm seed`) в автостарт **не входит** — сознательно, чтобы не перезатирать прод-данные при каждом рестарте.

Регресс на оба фикса чист (`tsc`, `check-catalog.mjs` 22/22, `check-orders.mjs` 46/46 — только для билд-фикса, деплой-фикс функциональность каталога/заявок не задевал). Детали и найденные попутно мелкие грабли non-root Docker-окружения (нет домашней директории у `nextjs`, `/app` был `root:root`, `pnpm`/corepack не работает внутри контейнера — используется прямой `tsx`, и т.д.) — `docs/CHANGELOG.md` (записи от 2026-08-18) и `docs/GOTCHAS.md`.

**Это два блокера, которые реально нашлись и закрылись при работе именно НАД контейнером/Docker-сборкой — оба вида проверок (build без БД, старт с пустой БД) теперь пройдены вживую, не в теории. Не факт, что это последние блокеры фазы 9 вообще** — до первого настоящего Coolify-деплоя (реальный VPS, реальные env, реальный Postgres-сервис Coolify, реальный домен) остаются непроверенными вещи вроде сетевой связности контейнер↔Postgres в конкретной Coolify-инсталляции, ограничений по памяти/CPU на VPS 3, поведения health-check'а Coolify на первом медленном старте (миграции + `next start` последовательно, не параллельно — старт может занять на несколько секунд дольше обычного). Первый реальный деплой — при пунктах 3–6 «Ждёт владельца» ниже — стоит считать первой настоящей проверкой, а не формальностью.

**Фаза 9 (инфраструктура продакшена) — закрыта 2026-08-18.** Пройдена не по сценарию ПРОМПТ 9 из `docs/PROMPTS.md` (тот писан под Hetzner + Cloudflare R2 + сгенерированный `server/setup.sh`) — по факту сделано вручную, пошагово, в чате-штабе на claude.ai, под реальные условия:

- **Хостинг: TopHost.md**, не Hetzner. VPS3 Unmanaged (4 ядра / 8 ГБ / 75 ГБ NVMe, Ubuntu 24.04.4 LTS), IP `195.178.106.241`. Важно на будущее: IP в панели TopHost (карточка услуги) и реальный IP на сетевом интерфейсе сервера **не совпадали** (панель показывала `195.178.156.241`, реальный — `195.178.106.241`, опечатка на уровне отображения панели) — при следующей операции с сервером сверять IP командой `ip -4 addr show` на самом сервере, не доверять карточке вслепую.
- **Домен:** `monflacon.md`, зарегистрирован тоже на TopHost (не отдельно). DNS переведён на **Cloudflare** (Free план) — две A-записи (root + www) на IP сервера, Proxied (оранжевое облако). Nameservers TopHost (`ns4`/`ns5.tophost.md`) заменены на Cloudflare (`tess.ns.cloudflare.com` / `uriah.ns.cloudflare.com`). Пропагация заняла меньше часа. **R2/S3 в Cloudflare НЕ подключался** — не было в объёме этой сессии, фото по-прежнему на диске VPS, как и планировалось изначально (см. «Организационный контекст» ниже).
- **Защита сервера (харднинг)** сделана вручную по шагам, не единым `setup.sh`: SSH-ключ (`ed25519`) сгенерирован и добавлен (`ssh-copy-id`), пароль **сознательно оставлен включённым** — решение владельца, не авто-отключение по плану; `ufw` (22/80/443/8000); `fail2ban` (сразу же забанил один автоматический сканер — обычный фоновый шум интернета, не связано с нашими попытками входа, подтверждено по `/var/log/auth.log`); `unattended-upgrades` (уже был включён дефолтом); swap 2 ГБ. Один незапланированный `reboot` для применения обновлённого ядра (6.8.0-117 → 6.8.0-137) — сервер один раз не поднялся сам после `reboot` из Ubuntu (потребовалась ручная кнопка Start в панели TopHost), затем поднялся штатно.
- **Coolify** (v4.3.1) установлен официальным скриптом напрямую на VPS (`this machine`, не отдельный провижининг через Hetzner/Vultr/DigitalOcean API — этих интеграций не было и не нужно). `.env` Coolify (внутренние секреты панели: `APP_KEY`, `DB_PASSWORD`, `REDIS_PASSWORD` и т.д.) выгружен и сохранён локально владельцем (`scp`) — **не в менеджере паролей**, лежит на диске в `B:\parfume_site_docs\upd-coolify-8-13\coolify-env-backup.txt`, стоит перенести в менеджер паролей при случае.
- **Postgres 17** (не 18-default) — отдельный сервис в Coolify, `Private` (не открыт наружу), подключение по внутреннему Docker-адресу.
- **Приложение** — тип ресурса `Public Git Repository` (репозиторий **сделан публичным** осознанным решением владельца, не приватным как предполагал ПРОМПТ 9 — `.env` в `.gitignore`, секретов в коде нет), `Build strategy: Dockerfile`, ветка **`phase_8.0`** (не `main` — та отстаёт, владелец решит вопрос слияния веток в GitHub отдельно), порт 3000. **Persistent volume** на `/app/media` подключён (Docker volume mount, без явного `Source Path` — управляется Coolify).
- **Домены в Coolify:** `https://monflacon.md` + `https://www.monflacon.md` привязаны к приложению, DNS Check — OK на оба, SSL выпущен автоматически.
- **Env-переменные заполнены в Coolify** (8 обязательных из ПРОМПТ 9, по факту): `DATABASE_URI` (Postgres internal URL), `PAYLOAD_SECRET` (сгенерирован `openssl rand -hex 32`), `ORDERS_DRY_RUN=0`, `SEED_ADMIN_EMAIL`/`SEED_ADMIN_PASSWORD` (`admin.monflacon@gmail.com` + сгенерированный пароль — **но см. ниже, реальный первый юзер создан другим способом**), `NEXT_PUBLIC_SERVER_URL=https://monflacon.md`, `TELEGRAM_CHAT_ID`/`TELEGRAM_BOT_TOKEN` (существующие боевые из фазы 4). Необязательные (`RESEND_API_KEY`, `NEXT_PUBLIC_GA_ID`, `SENTRY_DSN`, `S3_*`, `ORDERS_RATE_LIMIT`) оставлены незаполненными — не блокируют, дозаполняются позже.
- **Первый деплой падал** (commit `08f6d81`, sitemap-билд-блокер — уже описан выше в этом файле и в `CHANGELOG.md`), **второй прошёл** (`8b11939`), но контейнер отвечал ошибкой сервера — `relation "homepage" does not exist" — деплой-блокер (автомиграция, тоже уже описан выше). **Третий деплой** (`1ae7025`, с фиксом автомиграции) — **успешен, сайт живой**.
- **Первый пользователь админки создан вручную через форму `create-first-user`** на `/admin`, не через `SEED_ADMIN_EMAIL`/`PASSWORD` из env — эти переменные сидом не воспользовались (сид не входит в автостарт, см. деплой-блокер выше), Payload вместо этого сам показал форму создания первого юзера любому, кто зайдёт на `/admin`, пока таблица `users` пуста. **Владелец закрыл это немедленно** — заполнил форму сам (`admin.monflacon@gmail.com`, роль **Admin**, не «Менеджер»), тем самым закрыв временную дыру (Payload перестаёт показывать `create-first-user` после первого созданного юзера). На будущее — если понадобится второй прод-деплой с нуля, между стартом контейнера и созданием первого юзера есть окно, когда `/admin` открыт кому угодно; стоит создавать первого юзера как можно быстрее после первого успешного деплоя.
- **Демо-товары на проде залиты (2026-08-18).** 12 товаров / 35 вариантов через `scripts/import.ts` — те же демо-данные, что на деве (`src/lib/seed/data.ts`), не новые/случайные. Два новых CSV в репозитории: `scripts/fixtures/demo-products.csv` (формат A, RO-контент), `scripts/fixtures/demo-translations.csv` (RU/EN переводы) — оба в `scripts/`, не в `docs/` (та исключена `.dockerignore`, `scripts/` копируется в прод-образ целиком). Команда на проде (Coolify Terminal, внутри контейнера): `./node_modules/.bin/tsx scripts/import.ts <файл>` (не `pnpm import` — не работает в контейнере, см. деплой-блокер выше). **Ограничения этого демо-наполнения, известны и приняты:** без фото (CSV-импорт поле `images` не трогает); бренды/категории — «сырые» заготовки auto-create (`Maison Orphee` без диакритики, без описаний/isFeatured) — поправить вручную через `/admin`, ~10 минут, не срочно; главная (Hero/Editorial, глобалы Homepage/Navigation/Settings) CSV-импортом не наполняется вообще, остаётся в дефолте — их трогает только `pnpm seed`, который в проде не работает.
  - **Кэш-нюанс после первого импорта, найден и решён.** `/catalog` временно показывал «0 из 0», хотя главная уже отображала товары корректно — Claude Code разобрал причину отдельной диагностической сессией (`docs/CHANGELOG.md` → «Диагностика — пустой `/catalog` после CSV-импорта на проде»): независимые ключи `unstable_cache` у `/catalog` и главной, `revalidateTag` внутри CLI-процесса `scripts/import.ts` не долетает до кэша уже запущенного сайта (тот же тредофф, что и раньше — `docs/GOTCHAS.md` «CLI не сбрасывает кэш витрины»). **Решение — не терминал, а `/admin`:** зайти, открыть любой товар, нажать «Сохранить» без изменений — хук коллекции сбрасывает кэш всей витрины мгновенно; сработало с первого раза, каталог сразу отдал все 12 товаров. Держать в голове на будущее — **после любого следующего CSV-импорта/`pnpm seed`/`migrate` на проде повторить этот же шаг**, иначе каталог до 5 минут (`TTL=300с` страховка) будет показывать устаревшее состояние.

**Сессия 2026-08-22/23 — боевой тест импорта на проде и что он за собой потянул.** Начиналось как пункт 3 из четырёх ручных действий («прогнать CSV через кнопку в админке»), но живой тест на реальных данных вскрыл цепочку вещей, которых не было в плане. Все они сделаны, смержены в `phase_8.0`, задеплоены и **владельцем на проде проверены**:

1. **Модель данных: `title` товара больше не `localized`** (миграция `20260822_140412_phase8_1_title_not_localized.ts`). Разведка перед миграцией показала: у 12/12 демо-товаров title был **дословно одинаков** во всех трёх локалях — «перевод» названий через `demo-translations.csv` был фиктивным дублированием, реально переводилось только описание. По уточнению у клиента: **названия товаров всегда на английском, одни на все языки; переводится только описание.** Бэкфилл автоматический (первая непустая локаль ro→ru→en), ручные FTS/trigram-индексы поиска перенесены внутри той же миграции (`migrate:create` их не видит — грабля записана в GOTCHAS). Колонка `title` убрана из формата CSV-переводов (`handle,locale,description`).
2. **Мультиязычные описания одним файлом.** Форматы A/B понимают колонки `description_ro` / `description_ru` / `description_en` — все три языка за один прогон, селектор «Локаль контента» на такой файл не влияет. Одиночная `description` осталась для обратной совместимости.
3. **Загрузка фото товаров ZIP-архивом** (`/api/media-import`, блок «Шаг 1» на `/admin/catalog-import`) + привязка колонкой `images` в CSV по точному имени файла. Два независимых действия, не одна кнопка: сначала архив, потом CSV. Распаковка в памяти (`adm-zip`, чистый JS), файл в Media заводится штатным `payload.create({file: Buffer})` без записи на диск. Лимиты 200 МБ/архив, 8 МБ/файл. Ненайденное имя — предупреждение в отчёте, импорт не прерывается. Непустая ячейка `images` заменяет список фото целиком, пустая — не трогает.
4. **Одноколоночный CSV заполняет все три локали** — но **только те, где описание пустое**; готовые переводы не затираются. Раньше две локали оставались пустыми, и на витрине у товара просто не было описания на этих языках. В отчёте — явное предупреждение, что текст продублирован и его надо перевести.
5. **Косметика админки:** тосты о сохранении (позиция `top-center`, 6с, через штатную опцию `admin.toast`, не подмену компонентов), кнопки массовых действий (Delete красный, Edit акцентный), общий `ReportPanel.tsx` для блоков отчётов ZIP и CSV — успех / dry-run / замечания / ошибка теперь визуально различимы (до этого владелец при тесте не отличил успешный боевой импорт от проверки: отчёты выглядели одинаково). Попутно найден и исправлен дефект тёмной темы (navy-акцент не читался на тёмном фоне).
6. **Тексты интерфейса на экране импорта** переписаны с технического языка на понятный менеджеру.

**Прод-баг, найденный этой же сессией и закрытый (2026-08-22): загрузка ЛЮБЫХ файлов в Media на проде не работала вообще.** Симптом — `400 There was a problem while uploading the file`, в логах `EACCES: permission denied, open 'media/<файл>.jpg'`. Диагностирован владельцем на VPS: persistent volume Coolify монтируется в `/app/media` **после** сборки образа, точку монтирования Docker создаёт от `root`, а процесс работает под `nextjs` (uid 1001) — build-time `chown` в Dockerfile её физически не закрывает. **Это не баг сегодняшних правок — он существовал с первого деплоя фазы 9**, просто фото на прод до 22 августа никто не грузил (демо-товары заливались CSV без фото). Временно снято руками (`docker exec -u root <app> chown -R 1001:1001 /app/media` — сработало мгновенно, подтвердив диагноз), постоянный фикс — в `docker-entrypoint.sh`: контейнер стартует под root, чинит владельца `/app/media`, затем сбрасывает привилегии через `setpriv` (`util-linux`, уже в базовом образе — `gosu`/`su-exec` не понадобились) и рекурсивно перезапускает себя. `USER nextjs` из Dockerfile снят намеренно; сам процесс приложения по-прежнему работает под uid 1001, не root (проверено через `/proc/1/status`). Идемпотентно, 4 рестарта подряд без ошибок. **Проверено на проде вживую после деплоя — фото грузятся.**

**Сессия 2026-08-24 — Resend подключён боевым.** Чисто ручная настройка внешних панелей в чате-штабе, Claude Code в этой сессии не привлекался. Порядок: Resend → Domains → Add domain `monflacon.md` (регион Ireland, Manual setup DNS) → 4 записи (DKIM TXT, MX `send`, SPF TXT `send`, DMARC TXT `_dmarc`) внесены в Cloudflare **серым облаком** (DNS only) → «I've already added the records» → статус Verified (~1-2 часа на распространение DNS в этой сессии) → API key создан с правами Sending access → в Coolify (Environment Variables, окружение Production, ранее эти три переменные не существовали вовсе — заведены с нуля, не редактировались) заполнены `RESEND_API_KEY`, `ORDER_EMAIL_TO=admin.monflacon@gmail.com`, `ORDER_EMAIL_FROM=noreply@monflacon.md` → redeploy → **тестовая заявка на проде, оба письма пришли** (менеджеру и клиенту). Лимиты бесплатного плана Resend не проверялись отдельно (владелец спросил, куда смотреть — Metrics/Settings → Billing в самом Resend, ориентир из памяти ~3000 писем/мес, стоит свериться в аккаунте при росте трафика). Пошаговый markdown-гайд по всей процедуре подготовлен и сохранён у владельца отдельно от репозитория.

**Открыто:** фаза 9 закрыта, демо-наполнение сделано, **фаза 8.1 закрыта и на проде**, **Resend закрыт**, **фаза 9.1 (UX-правки витрины) закрыта**. Ручная приёмка (Приложение A, 10 ★-пунктов **+ три новых пункта под фазу 9.1**, см. «Ждёт владельца» п.6) на реальном телефоне на прод-домене — **всё ещё не пройдена**, теперь возможна целиком, включая два пункта, которые раньше нельзя было проверить (боевой Telegram, https без mixed content). Идёт после оставшихся двух ручных действий владельца (Шаг 2: GA4, бэкапы).

**Схема веток (зафиксирована 2026-08-20).** Staging-окружения нет и не будет — нет ресурсов на второй ресурс/базу в Coolify, это осознанное решение. Вместо него:

- **`main`** — актуален, в него влита `Phase_9.0_Latest_Merge_for_Main_20_august_work_version` (20.08). Не деплоится.
- **`phase_8.0`** — **ветка, которую деплоит Coolify**. Только сюда мержится готовое.
- **`phase_8.1_adding_features`** — рабочая ветка, в ней сделана вся фаза 8.1. **Смержена в `phase_8.0` 2026-08-20, задеплоена, проверена на проде.** Не удалена.
- **`phase_8.0_changing_locale`** — рабочая ветка сессии 22–23.08 (title/описания/фото/косметика + фикс прав на volume). **Смержена в `phase_8.0` 2026-08-22 и 2026-08-23, задеплоена, проверена на проде.** Не удалена — следующая задача продолжается в ней либо заводится новая.
- **Разовая настройка при мерже:** локальная `phase_8.0` не была связана с удалённой (`git pull` ругался «no tracking information») — вылечено `git branch --set-upstream-to=origin/phase_8.0 phase_8.0`, впредь `git pull` на ней работает сам.
- **Правило:** ветку в самом Coolify не переключать. Прод зависит от одной ветки, всегда `phase_8.0`.
- **Долг:** три состояния (`main` / `phase_8.0` / рабочая) рано или поздно надо схлопнуть в одну прод-ветку (`main` = прод, как в PLAN.md §10.5). Не срочно, но чем дальше — тем дороже.
- **Что локальный тест не ловит** (проверено на своей шкуре в фазе 9, два блокера): Docker-сборку, автомиграции, env-специфику Coolify. Эти вещи проверяются только реальным деплоем — по-прежнему деплоить с осторожностью, не под конец рабочего дня.

**Пробелы, найденные разбором 2026-08-20 (не баги, а неподключённые вещи):**

1. **Бэкапов фактически всё ещё нет — но механизм готов (фаза 8.1, задача 1, закрыта 2026-08-20).** В репозитории: `scripts/backup-media.sh` (динамический поиск volume на хосте через `docker inspect`, без хардкода id контейнера; архив вне докерных volume'ов; ротация N последних, по умолчанию 14; `flock`; лог; ненулевой exit при ошибке; закомментированный `rsync`-по-SSH блок на случай появления внешнего хранилища), `docs/backup.md` (что/где/как часто + готовая cron-строка), `docs/restore-drill.md` (проверка восстановлением). Claude Code проверил только синтаксис (`bash -n`) и исполняемый бит — **вживую скрипт ни разу не запускался, у Claude Code нет ssh к VPS.** Не сделано и делает владелец руками: перенос скрипта на хост VPS, первый ручной прогон, установка cron, расписание Postgres-бэкапов в Coolify. **До этого момента риск остаётся полностью открытым — есть механизм, но нет ни одной резервной копии.**
   - **Нюанс переноса, разобран в штабе:** скрипт приезжает на сервер вместе с деплоем, но **внутрь контейнера**, а работать должен **на хосте** (ему нужен `docker inspect`, изнутри контейнера это не сработает). Владелец переносит его на хост вручную (`scp` из WSL либо копипаст через `nano`), `chmod +x`, первый прогон руками, и только потом cron. Cron владелец тоже ставит вручную, не скриптом.
2. ~~**Письма не уходят.**~~ — ✅ **закрыто 2026-08-24.** `RESEND_API_KEY` заполнен в проде, домен верифицирован, оба письма (менеджеру и клиенту) на боевой заявке проверены живьём. Детали — раздел «Сессия 2026-08-24» выше.
3. **Аналитики нет.** `NEXT_PUBLIC_GA_ID` пуст → GA4 не собирает ничего, cookie-баннер не показывается (спрашивать не о чем). Код фазы 7.5 готов, нужен GA4-ресурс + Measurement ID + **полный redeploy** (переменная `NEXT_PUBLIC_`, подхватывается на сборке, рестарта недостаточно). Инструкция выдана в чате-штабе 20.08. **Владелец берёт вторым пунктом.** Приятный побочный эффект закрытия линт-долга (см. п.7 «Ждёт владельца»): `ConsentBanner.tsx` и `GoogleAnalytics.tsx` при рефакторинге были проверены вживую с тестовым `G-...` — цепочка «баннер → согласие → загрузка gtag.js → reload без повторного баннера» подтверждена Playwright'ом, так что при подстановке боевого ID сюрпризов быть не должно.
4. **CSV-импорт только через терминал контейнера — ✅ закрыто (фаза 8.1, задача 2, 2026-08-20), на проде проверено владельцем: кнопка импорта CSV и кнопка сброса кэша в админке видны и доступны.** Экран `/admin/catalog-import` (выбор файла, режим dry-run/боевой, отчёт на экране в том же формате, что печатал CLI) + пункт в навигации админки + явная кнопка «Сбросить кэш витрины». Оба эндпоинта (`/api/catalog-import`, `/api/catalog-revalidate`) — только роль admin, менеджеру 403 и пункта меню не видно (проверено Claude Code под временным manager-юзером). Движок импорта переиспользован из `src/lib/import/` как есть, второй парсер не писался, CLI `scripts/import.ts` не тронут. **После реального импорта через форму кэш витрины сбрасывается автоматически** — ручной трюк «открыть товар → Сохранить» для этого пути больше не нужен (проверено замером: свежие данные видны за 0.1–1с). Кнопка сброса остаётся нужной для CLI-импорта и ручных правок в БД. **Боевой тест на проде пройден владельцем 2026-08-22/23** — см. следующий пункт.
   - **Боевой тест (2026-08-22/23) — что реально проверено и что нет.** Проверено вживую на проде: dry-run и реальный импорт через форму; автосброс кэша (товар появился на `/catalog` в инкогнито сразу, без ручного «Сохранить»); загрузка ZIP с фото и привязка колонкой `images` (6 файлов, все нашлись); трёхколоночные описания. **Тестировали не `scripts/fixtures/demo-products.csv`, как планировалось, а специально сгенерированные тестовые файлы** (`TEST-*` handle/SKU) — так было безопаснее, повторный прогон демо-файла на проде не делался. **НЕ проверено владельцем: недоступность экрана импорта менеджеру** (роль проверял только Claude Code на деве под временным юзером) — стоит проверить, когда будет заведён реальный менеджерский аккаунт. **Тестовые товары `TEST-*` и загруженные тестовые фото на проде остались — их нужно удалить** (`/admin/collections/products` и `/admin/collections/media`, поиск по «TEST»).
   - Попутно Claude Code нашёл и исправил два реальных бага, не пометив их как ограничения: (1) Payload не оборачивает кастомные top-level admin-views в штатный layout сам — обёрнуто вручную `DefaultTemplate`; (2) `user` в пропсах такого view лежит не там, где предполагалось — из-за чего первая версия отказывала в доступе даже настоящему admin. Оба задокументированы в `docs/GOTCHAS.md`.

**Фаза 9.1 — пакет UX-правок витрины по замечаниям клиента, закрыта 2026-08-27.** Пять независимых задач одной сессией Claude Code, пять отдельных коммитов. Регресс зелёный (`tsc`, `lint`, `pnpm migrate`, `test:int` 23/23, `check-catalog.mjs` 22/22, `check-orders.mjs` 54/54 — было 46, `test:e2e` 20/20 — было 19). Проверки, которые ломались от переезда фильтров в дровер, **обновлены, а не ослаблены**. `CHANGELOG.md` и `GOTCHAS.md` дописаны. Мерж в `phase_8.0` и деплой — по обычной схеме, git делает только владелец.

1. **Фильтры каталога — единый дровер по кнопке на всех разрешениях.** Постоянный сайдбар убран совсем, сетка занимает всю ширину. `MobileFilters.tsx` → `FiltersDrawer.tsx`: один компонент на все ширины, различается только геометрия — bottom-sheet на <1024, боковая панель справа на ≥1024. Кнопка «Фильтр» с бейджем активных фильтров уже была в проекте, переиспользована как есть; слои истории `useSheetLayers` (кнопка «Назад» на телефоне) сохранены целиком. Закрытие: X, клик по фону, «Показать результаты», «Назад». Layout shift исключён двумя мерами: дровер — fixed-оверлей поверх контента, плюс `scrollbar-gutter: stable` на `html` (без него блокировка `body{overflow:hidden}` убирала полосу прокрутки и дёргала страницу по ширине). Подтверждено замером `boundingBox()` карточки до / во время / после открытия — 32 / 325.25 во всех трёх точках. **Единое поведение на десктопе и мобильном — решение владельца, зафиксировано в штабе 28.08.**
2. **Тост «Добавлено в корзину».** Стор `src/lib/ui/toast.ts` (один тост, повторное добавление перезапускает таймер — та же механика, что у тостов сохранения в `/admin`) + `CartToast.tsx` в layout витрины. Navy/cream по токенам, миниатюра + название + объём, автоскрытие 2,6 с, `role="status"`. Не блокирует страницу: `pointer-events-none` и на контейнере, и на плашке — `elementFromPoint` под тостом отдаёт страницу, не тост.
3. **Заявка: «Способ оформления» + «Адрес».** `CHECKOUT_MODES = ['standard','noCall']` и `address` добавлены в существующую `orderRequestSchema` (второй схемы не заводилось), поля `checkoutMode` и `customer.address` — в коллекцию `Orders`, миграция `NOT NULL DEFAULT 'standard'` (старые заявки бэкфилятся сами). **Телефон обязателен в обоих режимах**, валидация не тронута — на это есть отдельная проверка в тестах. Пометка «не звонить» видна во всех четырёх местах: Telegram, письмо менеджеру (строка в теле + суффикс в теме), `/admin` (сайдбар заказа + колонка в списке), CSV.
   - **Адрес сделан опциональным** — это решение штаба по умолчанию, **владелец его пока не подтвердил** (см. «Ждёт владельца» п.10). Если доставка без адреса бессмысленна — поле переводится в обязательные, и это Zod + `required: true` + новая миграция, то есть не косметика.
   - **Одно отклонение от задания, принято:** в CSV шапка фиксированная, поэтому пустой адрес — **пустая ячейка**, а не исчезающая колонка. В Telegram и письме пустой адрес опускается целиком, как и просили.
4. **Пропавший поиск по коду заказа — UI-регрессия, ссылка вернулась.** Функциональность всё это время была цела: `/[locale]/order`, `OrderLookupForm`, `/api/order-status`, `/order/[token]` на месте, прод отдавал `https://monflacon.md/ro/order` со статусом 200 и рабочей формой. **Виноват не коммит, а контент:** ссылка «Отследить заказ» рендерилась внутри `columns.map()` футера под условием `index === CUSTOMERS_COLUMN_INDEX` (1) — то есть существовала, только пока в CMS заполнена вторая колонка футера. На проде `navigation.footerColumns` пуст (в футере только хардкодная «Contacte»), цикл не выполнялся, ссылка исчезала целиком. Исправлено: ссылка вынесена из цикла во всегда отрисовываемую колонку контактов, `CUSTOMERS_COLUMN_INDEX` удалён, в `check-orders.mjs` добавлена проверка, чтобы это не повторилось. **Класс бага, который стоит помнить: UI-элемент, отрендеренный внутри CMS-цикла, исчезает вместе с пустым контентом — код при этом чистый и git-история ничего не покажет.**
5. **«Сайт грузил компьютер, картинка прыгала» — воспроизведено и починено.** Механизм: sticky-шапка остаётся в потоке; `useScrolled(40)` сжимает её паддинги и знак, высота контента выше позиции уменьшается на ~14px, браузерный scroll anchoring компенсирует это сдвигом `scrollY`, порог пересекается обратно, шапка разжимается — и цикл повторяется каждый кадр. Подтверждено замером: на старом коде парковка скролла на 42/44/46px давала самопроизвольное гуляние позиции на 37–45px кадр за кадром. Фикс — гистерезис ±24px (мёртвая зона заведомо шире дельты шапки); после него прогон 0–120px с шагом 2px даёт ноль нестабильных позиций, longtask > 50ms нет ни на главной, ни в каталоге, ни в карточке. Остальные подозреваемые проверены и чисты: `requestAnimationFrame`, `setInterval`, `IntersectionObserver`, `scrollIntoView`; каруселей и автоплея в коде нет вовсе; единственный слушатель скролла — этот, passive; lazy loading не откатывался (`priority` только у hero, первой картинки `ProductRow` и первых 4 карточек каталога).

**Что по фазе 9.1 осталось на владельце:** глазами проверить дровер фильтров и тост корзины **с реального телефона** — единственный пункт самопроверки, который Claude Code закрыть не мог. Идёт в общий пакет ручной приёмки.

**Фаза 6 (дизайн-интеграция) — заморожена.** Текущий дизайн (главная, шапка, футер, эталонная карточка + рескин фазы 5) принят как есть; полный ПРОМПТ 6 с рескином остальных страниц НЕ запускается. Дальше по дизайну — только точечные правки, когда владелец обсудит с клиентом и составит мини-ТЗ; вносятся служебным промптом «Правки дизайнера» из `docs/PROMPTS.md` одним списком, не фазой 6 целиком.

**Важное отклонение от плана фазы 5 (зафиксировано в `PLAN.md` §5.2 и `CHANGELOG.md`):** `loading.tsx` сознательно НЕ добавлен — общий loading-индикатор сегмента ломает HTTP-статус `notFound()` ниже по дереву (стриминг Suspense отправляет `200` раньше, чем разрешится `404`; поймано регрессом `check-orders.mjs`, воспроизведено и в dev, и в прод-сборке, задокументировано в `docs/GOTCHAS.md`). Корректный статус 404 важнее общего лоадера при переходах.

## ЧТО ДАЛЬШЕ

### Шаг 0 — проверка перед стартом (на деве, локально)

```bash
cd ~/projects/parfume_site
git status   # .env НЕ коммитить — в нём реальный токен бота, должен быть в .gitignore
node -v && pnpm -v          # v22.x / 11.x
docker compose version
claude --version            # 2.1.x
head -30 docs/CHANGELOG.md  # последние записи: деплой фазы 9 + автомиграции + багфикс sitemap.ts
```

### Шаг 1 — фаза 8.1 ✅ ЗАКРЫТА (2026-08-20)

Обе задачи сделаны в `phase_8.1_adding_features`, промпты давались **по очереди** (а не разом — каждый заканчивается ритуалом, совместная выдача склеила бы две несвязанные темы в один коммит). Смержено в `phase_8.0`, задеплоено, кнопки на проде владельцем проверены.

1. **Бэкапы** (`scripts/backup-media.sh`, `docs/backup.md`, `docs/restore-drill.md`) — ✅ код и документация. Рантайм не тронут. **Включение на сервере — за владельцем, см. Шаг 2.**
2. **Кнопки в админке** (`/admin/catalog-import` + сброс кэша витрины) — ✅ полностью, с локальной проверкой Claude Code: `generate:importmap`, `tsc --noEmit`, `build` (exit 0), `check-catalog.mjs` 22/22, `check-orders.mjs` 46/46, `test:e2e` 19/19, живой `pnpm build && pnpm start` со скриншотами, проверка ролей под временным manager-юзером, боевой импорт `scripts/fixtures/demo-products.csv` (dry-run → реальный), замер автосброса кэша, уборка тестовых данных после себя. Линт-долг фазы 7 корректно не тронут и назван отдельно (см. «Ждёт владельца» п.7 — всё ещё открыт).

### Шаг 1б — фаза 9.1 ✅ ЗАКРЫТА (2026-08-27)

Пакет UX-правок витрины по замечаниям клиента: фильтры в дровер по кнопке на всех разрешениях, тост корзины, «Способ оформления» + «Адрес» в заявке, возврат ссылки на отслеживание заказа, фикс автоскролла sticky-шапки. Подробности — раздел «Фаза 9.1» в «Где мы» выше и `docs/CHANGELOG.md` (запись от 2026-08-27). Два хвоста на владельце: подтвердить решение по обязательности адреса и проверить дровер/тост с реального телефона (оба — в «Ждёт владельца»).

### Шаг 2 — ручные действия владельца (порядок его собственный, зафиксирован 20.08; пункты 3 и 1 закрыты — 22–23.08 и 24.08, осталось два)

Кода не требуют, Claude Code их выполнить физически не может (нет ssh к VPS, нет доступа к внешним панелям).

1. ~~**Resend**~~ — ✅ **закрыт 2026-08-24.** Домен верифицирован, API-ключ и все три env-переменные заполнены в Coolify, redeploy сделан, тестовая заявка подтвердила оба письма (менеджеру и клиенту). Пошаговый гайд сохранён у владельца. Детали — раздел «Сессия 2026-08-24» выше.
2. **GA4** — property (Chișinău, MDL) → Web data stream на `https://monflacon.md` → Measurement ID `G-...` → в Coolify env `NEXT_PUBLIC_GA_ID` → **полный redeploy** → проверка в Realtime, **обязательно приняв cookie-баннер** (без согласия скрипты не грузятся, это by design). ~15 мин.
3. ~~**Боевой тест кнопки импорта CSV**~~ — ✅ **пройден 2026-08-22/23.** Dry-run и реальный импорт через форму, автосброс кэша, ZIP с фото + колонка `images` — всё работает на проде. Тестировали на сгенерированных `TEST-*` файлах, не на `demo-products.csv`. Осталось два хвоста: удалить тестовые товары/фото с прода и когда-нибудь проверить недоступность экрана менеджеру (см. «Ждёт владельца»).
4. **Бэкапы — включение** (владелец делает **вручную, не автоматизируя**): перенести `scripts/backup-media.sh` на **хост** VPS (`scp` из WSL или копипаст через `nano`; напоминание — на хост, не в контейнер, скрипту нужен `docker inspect`), `chmod +x`, **первый прогон руками** и глазами убедиться, что архив создался, затем **вручную прописать cron** строкой из `docs/backup.md`. Отдельно — Coolify → сервис Postgres → Backups → расписание `0 3 * * *`, retention 30 (хранение: Cloudflare R2 free через Coolify Settings → S3 Storages, либо локально на VPS как временный минимум).

**Не срочно, идёт в пакете с GA4:** Search Console — подтвердить домен, скормить `https://monflacon.md/sitemap.xml`.

### Шаг 3 — ручная приёмка (Приложение A, 10 ★-пунктов + 3 новых)

Демо-наполнение сделано (12 товаров/35 вариантов, см. раздел «Фаза 9» выше). Прод-домен и боевые каналы наконец доступны — можно пройти все 10 пунктов, включая два, которые раньше нельзя было проверить в деве (заявка доходит в боевой Telegram, https без mixed content). С реального телефона, на мобильном интернете, не по Wi-Fi. Баги — списком в чат-штаб.

**Приложение A нужно дополнить — фаза 9.1 добавила функциональность, которой в старом списке из 10 пунктов нет.** Три новых проверки:

- **11.** Кнопка «Фильтр» открывает и закрывает фильтры **и на телефоне, и на десктопе**; сетка товаров при этом не дёргается; кнопка «Назад» на телефоне закрывает дровер, а не уводит со страницы.
- **12.** Заявка со «Способ оформления → Заказать без звонка» уходит и пометка «не звонить» видна во **всех четырёх** каналах: сообщение в Telegram, письмо менеджеру, карточка заказа в `/admin`, выгрузка CSV. Телефон при этом по-прежнему требуется — форма не должна пропускать заявку без него ни в одном режиме.
- **13.** Ссылка на отслеживание заказа видна в футере на всех трёх локалях, страница открывается, поиск по коду находит заявку. Проверять именно с прода — на деве футер может быть наполнен контентом, которого на проде нет, и баг снова спрячется.

**Перед началом:** если между этой сессией и приёмкой прошло время и был любой новый CSV-импорт/`pnpm seed`/`migrate` на проде — сначала зайти в `/admin`, сохранить любой товар без изменений (сбрасывает кэш витрины, см. кэш-нюанс в разделе «Фаза 9»), иначе `/catalog` может на короткое время показывать устаревшее состояние.

### Дальше — фаза 10

`docs/PROMPTS.md` ПРОМПТ 10 писан под другой сценарий деплоя (Hetzner + `docs/go-live-checklist.md`, которого в этом проекте не было) — использовать его как ориентир по духу (прод-smoke, финальные security-заголовки, переход на схему `main`=прод), но не буквально по шагам; фактическая инфраструктура и её детали — в этом файле (раздел «Фаза 9» выше), не в `go-live-checklist.md`.

**Restore-drill — не пропускать.** `docs/restore-drill.md` готов (фаза 8.1, задача 1). Пройти его вживую один раз **после** того, как заработают cron бэкапа медиа и расписание Postgres в Coolify (Шаг 2, п.4), и поставить напоминание раз в месяц. Бэкап, который ни разу не разворачивали, бэкапом не считается (PLAN.md §11.2). До первого пройденного drill'а считать, что бэкапов нет.

Фаза 6 встраивается точечными правками, когда будет ТЗ по дизайну от клиента.

**Наполнение реальными товарами от клиента** по-прежнему снято с критического пути: у клиента пока нет ни товаров, ни данных 1С. Демо-наполнение (шаг 1 выше) — временное, только чтобы клиенту было что показать; реальный прайс заливается лёгким CSV в любой момент, когда появится.

## Ждёт владельца

| # | Что | Когда | Блокирует |
|---|---|---|---|
| 1 | **Правки дизайна идут через дизайнера в Figma** (решение 2026-08-28). Клиент запросил много правок, подключён свой дизайнер. Схема: страницы прода импортируются в Figma плагином прямо по URL (`html.to.design` / `html2design`, бесплатной Figma хватает — экспорт сайта в HTML **не нужен, никакой работы для Claude Code тут нет**), дизайнер правит, отдаёт **не Figma-файл, а спеку**: изменённые значения токенов `docs/BRAND.md` + скриншоты с пронумерованными выносками. Обратный путь Figma→код не используется — он платный (Dev seat) и всё равно не ложится на существующие компоненты. Спека → служебный промпт «Правки дизайнера» из `docs/PROMPTS.md`, одним списком. Полный рескин (фаза 6) по-прежнему не делаем | в работе | точечные дизайн-правки (не блокирует остальные фазы) |
| 2 | **1С / товары** — у клиента пока нет ни товаров, ни артикулов 1С. Снято с критического пути: запуск возможен без реального прайса, каталог наполняется лёгким CSV в любой момент (импорт готов). Когда товары появятся — согласовать `sku` с учётом клиента, чтобы не пересопоставлять сотни позиций вручную. Экспорт заказа в 1С — позже, формат спросить у 1С-ника | когда у клиента появятся товары | ничего сейчас |
| 3 | ~~**① Подключить Resend боевым**~~ — ✅ **закрыто 2026-08-24.** Домен верифицирован в Resend (SPF/DKIM/DMARC в Cloudflare, серое облако), API-ключ создан (Sending access), `RESEND_API_KEY`/`ORDER_EMAIL_TO`/`ORDER_EMAIL_FROM` заполнены в Coolify env (Production, заведены с нуля), redeploy сделан, тестовая заявка на проде — оба письма пришли (менеджеру на `admin.monflacon@gmail.com`, клиенту на email из формы). Пошаговый гайд по всей процедуре сохранён у владельца (markdown, вне репозитория) | ✅ сделано | — |
| 3a | **② Подключить GA4** — property → web stream → `G-...` → `NEXT_PUBLIC_GA_ID` в Coolify → **полный** redeploy (рестарта мало, переменная `NEXT_PUBLIC_`) → проверка в Realtime с принятым cookie-баннером. Инструкция — чат-штаб 20.08. ~15 мин | **сейчас, пункт 1 из 2** | всю аналитику посещаемости и cookie-баннер |
| 3b | ~~**③ Боевой тест кнопки импорта CSV на проде**~~ — ✅ **пройден 2026-08-22/23.** Dry-run + реальный импорт через форму, автосброс кэша подтверждён (товар виден в инкогнито сразу), ZIP с фото и привязка `images` работают. По ходу теста найдена и закрыта цепочка задач (см. «Где мы» → сессия 22/23.08) и **реальный прод-баг: загрузка в Media вообще не работала из-за прав на `/app/media`** — постоянный фикс задеплоен и проверен | ✅ сделано | — |
| 3b-1 | **Удалить тестовые данные с прода** — товары с handle/SKU `TEST-*` (`TEST-WOOD-SPACE`, `TEST-ROSE-LIGHT`, `TEST-AMBER-NIGHT`, `TEST-ALPHA`, `TEST-BETA`, `TEST-CACHE-CHECK` и т.п.) и загруженные тестовые фото в Медиа. Искать по «TEST» в `/admin/collections/products` и по именам файлов в `/admin/collections/media`. ~5 минут | до показа клиенту | показ клиенту / ручную приёмку |
| 3b-2 | **Проверить, что экран `/admin/catalog-import` недоступен менеджеру** — на деве Claude Code проверял под временным manager-юзером, на проде владелец не проверял. Сделать, когда будет заведён реальный аккаунт менеджера | когда появится менеджер | нет |
| 3c | **③ Включить бэкапы — вручную, владелец делает сам без автоматизации:** перенести `scripts/backup-media.sh` на **хост** VPS (`scp` или копипаст через `nano`; на хост, не в контейнер — нужен `docker inspect`), `chmod +x`, **первый прогон руками** с проверкой что архив создался, затем **вручную прописать cron** строкой из `docs/backup.md`. Плюс Coolify → Postgres → Backups → `0 3 * * *`, retention 30 (R2 free или локально на VPS как минимум). Скрипт и инструкция готовы (фаза 8.1) | **сейчас, пункт 2 из 2** | сохранность данных — **главный открытый риск, пока не сделано** |
| 3d | **Пройти `docs/restore-drill.md` вживую один раз** после 3c и поставить напоминание раз в месяц. До первого drill'а бэкапы не считаются существующими | после 3c | сдачу фазы 10 / 11 |
| 4 | **Переслать `docs/translations-review.md`** на вычитку: RO — клиент/носитель, EN — ты или дизайнер. Отдельно на страницах Доставка/Возврат (`/admin/collections/pages`) проверить не только язык, а реальные условия (сроки, оплата, окно возврата 14 дней) — сейчас стартовый черновик | можно уже сейчас | вычитку переводов (фаза 11) |
| 5 | **Обновить чек-лист клиента** (v3): этап 4 закрыт, этап 5 (акции = фаза 4.5) закрыт, поток заявки и отслеживание (4.6/4.7) закрыты, контент-страницы и бренды (фаза 5) закрыты, SEO и производительность (фаза 7) закрыты, тестирование (фаза 8) закрыто, **инфраструктура и деплой (фаза 9) закрыты — прод живой** | не срочно | нет |
| 5a | **Черновики документов клиенту готовы** (чат-штаб 20.08, у владельца, не в репо): карта сайта + карта админки + описание функционала сайта и админки; черновая инструкция по админке без скриншотов. **Обе меняющие админку задачи фазы 8.1 теперь закрыты — блокер для финального `docs/ADMIN-GUIDE.md` снят**, его можно заказывать Claude Code (фаза 11). **После сессии 22–23.08 гайд должен покрывать больше:** экран `/admin/catalog-import` целиком (Шаг 1 — ZIP с фото, Шаг 2 — CSV), кнопку сброса кэша, новый формат CSV (`description_ro/ru/en`, колонка `images`), правило «названия товаров всегда на английском, переводится только описание», и требования к фото для клиента (квадрат **не меньше** 1200×1200, до 1 МБ, имена файлов совпадают с CSV до буквы). Черновик по админке тоже стоит дополнить | когда удобно | нет |
| 6 | **Ручная приёмка (Приложение A, ★-шпаргалка из 10 пунктов + 3 новых)** — теперь полностью возможна, прод-домен, боевые каналы и демо-товары есть. Шпаргалка передана в чате 2026-08-12, сохрани её себе, тут не дублируется. **Фаза 9.1 добавила три пункта (11–13): дровер фильтров на телефоне и десктопе, заявка «без звонка» с пометкой во всех 4 каналах, ссылка на отслеживание заказа в футере** — формулировки в разделе «Шаг 3» выше, впиши их в свою шпаргалку до прохода, иначе новая функциональность проверена не будет. Пройти все пункты вживую с телефона на мобильном интернете (не по Wi-Fi), баги списком в чат-штаб | ближайшая сессия | сдачу фазы 10 |
| 7 | ~~**Линт-долг фазы 7**~~ — ✅ **закрыт 2026-08-20.** `pnpm lint` — 0 ошибок, 0 предупреждений; ESLint внутри `pnpm build` тоже чист. Разобрано по существу, **без единого `eslint-disable` и без ослабления конфига**: `NewOrdersCard.tsx` — `<a>` → `<Link>` (7× `no-html-link-for-pages`, серверный компонент, `Link` в RSC работает штатно); `ConsentBanner.tsx` + `GoogleAnalytics.tsx` — `react-hooks/set-state-in-effect`, оба читали `localStorage` через `useEffect`+`setState` на монтировании → переведены на `useSyncExternalStore` (это применение уже задокументированной в `docs/GOTCHAS.md` конвенции, не новый паттерн); `JsonLd.tsx` — мёртвый `eslint-disable` для правила, которого нет в пресете, убран. Регресс чист (22/22, 46/46, e2e 19/19), поведение баннера согласия и загрузки GA4 **проверено вживую Playwright'ом** с тестовым `NEXT_PUBLIC_GA_ID` (баннер до согласия → gtag.js не грузится → «Принять» прячет баннер и грузит скрипт → reload не показывает баннер повторно). Тестовые заявки после прогонов убраны | ✅ сделано | — |
| 8 | **Перенести `.env` Coolify в менеджер паролей** — сейчас лежит только на диске (`B:\parfume_site_docs\upd-coolify-8-13\coolify-env-backup.txt`), единственная копия для восстановления Coolify при переустановке сервера | когда удобно | нет, но риск потери при поломке диска |
| 9 | **Косметика демо-данных для показа клиенту (необязательно)** — бренды/категории после CSV-импорта остались «сырыми» заготовками auto-create (`Maison Orphee` без диакритики, без описаний). Поправить через `/admin`, ~10 минут | когда удобно, до показа клиенту если хочется чище | нет |

| 10 | **Решить, обязателен ли «Адрес» в заявке.** Сейчас поле опциональное — это дефолт от штаба, Claude Code явно просил подтвердить у владельца. Если для доставки адрес обязателен, это не косметика: Zod + `required: true` в коллекции `Orders` + новая миграция. Решать до реального запуска, потом переводить поле в обязательные на живых заявках дороже | до запуска / показа клиенту | корректность потока заказа |
| 11 | **Проверить дровер фильтров и тост корзины с реального телефона** — единственный пункт самопроверки фазы 9.1, который Claude Code закрыть не мог (у него нет устройства). Идёт в общий пакет ручной приёмки, п.6 | вместе с приёмкой | сдачу фазы 10 |
| 12 | **Снять `MAINTENANCE_MODE` перед реальным запуском.** В Coolify → `MAINTENANCE_MODE=0` + полный **Redeploy** (не рестарт). Сразу после — **Purge Everything в Cloudflare**, иначе часть посетителей по кэшу edge продолжит видеть заглушку. Порядок именно такой: сброс режима → redeploy → purge, не наоборот | день реального запуска | посетители видят заглушку вместо рабочего сайта |
| 13 | **Вписать TikTok, когда клиент даст ссылку.** Переменная `NEXT_PUBLIC_SOCIAL_TIKTOK` подставляется на этапе **сборки** — просто вписать значение в Coolify недостаточно, нужен полный Redeploy (рестарта мало) | когда клиент даст ссылку | ничего срочного — кнопка просто отсутствует до этого |

## Организационный контекст

- **Хранилище фото:** диск VPS (persistent volume `/app/media` в Coolify), S3/R2 — потом, если понадобится (переключается адаптером Payload).
- **Email заявок:** Resend, подключён боевым 2026-08-24 — домен `monflacon.md` верифицирован, `RESEND_API_KEY`/`ORDER_EMAIL_TO`/`ORDER_EMAIL_FROM` заполнены в Coolify. Письмо менеджеру (фаза 4) и клиенту (4.6) работают на проде, проверено живой тестовой заявкой (оба письма пришли).
- **Telegram:** работает боевым и на деве, и на проде (проверено на деве вживую в фазе 4; на проде `ORDERS_DRY_RUN=0` — заявки реально уходят, боевая проверка через ручную приёмку, см. «Ждёт владельца»). Бот `@mooflaco_order_bot`, `TELEGRAM_CHAT_ID=799511005`, токен в `.env` на деве и в env-переменных Coolify на проде.
- **Регистрация / личные кабинеты:** не нужны, только гостевой заказ. Отслеживание — по токену или номер+телефон, без ЛК.
- **Оплата:** онлайн нет. Позже — Paynet или эквайринг maib / Victoriabank. Stripe в Молдове недоступен.
- **Документы клиенту** (у владельца, не в репо): презентация v7 (docx + pdf), чек-лист v3 (docx + pdf). Цена проекта — **1 200 €** единоразово, поддержка 2 месяца включена.

## Карта документации

| Файл | Отвечает на вопрос | Кто пишет |
|---|---|---|
| `CLAUDE.md` | как работать: правила, запреты, стек, ритуал | владелец |
| `docs/STATE.md` | **где мы и что дальше** (этот файл) | владелец / чат-штаб |
| `docs/GOTCHAS.md` | грабли и найденные решения | Claude Code дописывает |
| `docs/CHANGELOG.md` | что сделано, по датам (+ `docs/archive/`) | Claude Code, ритуал завершения |
| `docs/PLAN.md` | спецификация фаз и критерии приёмки | владелец |
| `docs/PROMPTS.md` | готовые промпты по фазам | владелец |
| `docs/BRAND.md`, `docs/WIREFRAMES.md`, `docs/mockups/` | дизайн-правда | владелец |
| `docs/import-guide.md`, `docs/design-brief.md`, `docs/client-questions.md` | операционка | владелец |

## Как обновлять этот файл

В конце сессии, если закрыта фаза/подфаза, изменился список «Ждёт владельца», сместился план или появился новый блокер:

1. Правится **только раздел «Где мы», «Что дальше» и «Ждёт владельца»** — остальное статично.
2. Дата в шапке — обязательно.
3. Файл сохраняется в репозиторий **и** перезаливается в файлы проекта claude.ai (одна и та же версия в двух местах, имя не меняется — никаких `-4`, `-5`).
