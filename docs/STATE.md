# STATE.md — где мы сейчас

> **Обновлён:** 2026-08-12, после фазы 8. **Следующий шаг:** ПРОМПТ 9 — инфраструктура продакшена.
>
> Единственная точка передачи между сессиями. Читают все: Claude Code (при старте сессии), чат-штаб на claude.ai (файл приложен в файлы проекта), владелец.
>
> **Правило записи: этот файл обновляет только владелец (руками или через чат-штаб).** Claude Code его читает, но никогда не редактирует и не создаёт.
>
> Здесь — только состояние. Правила работы → `CLAUDE.md` · спека → `docs/PLAN.md` · промпты → `docs/PROMPTS.md` · грабли → `docs/GOTCHAS.md` · история → `docs/CHANGELOG.md`. Ничего из этого сюда не копировать.

## Проект в двух строках

Сайт-каталог парфюмерии **MON FLACON** для рынка **Молдовы**. Витрина без онлайн-оплаты: карточки товаров, фильтры, корзина, форма заявки. ~100+ товаров × 5–7 объёмов → модель `Product → Variants` (~600 SKU). Локали **RO / RU / EN**, валюта **MDL**. Сборку делает **Claude Code** (терминал WSL), чат claude.ai — штаб: планирует, разбирает ошибки, готовит документы клиенту. Проект локально: `~/projects/parfume_site`.

## Где мы

**Закрыто:** фазы 0 → 4.7.x включительно, багфикс UX 4.x (вспышка пустой корзины на пути «Отправить заявку → Спасибо»), добор к фазе 5 (точка входа в отслеживание заказа из UI), **фаза 5** (главная из CMS по вайрфреймам/мокапу, коллекция Pages со статикой О нас/Доставка/Возврат/Контакты, `/brands` + страницы брендов, select→relationship на промо-ссылках и меню шапки с миграцией старых значений, кастомные 404/error на 3 локалях, ревизия переводов + `docs/translations-review.md`), **фаза 7** (SEO + производительность) и **фаза 8** (тестирование — см. ниже). Каталог, корзина, заявки, акции, поток заявки, отслеживание заказа, админка, контентные страницы, SEO-разметка и тестовое покрытие — работают. Детали каждой фазы — `docs/CHANGELOG.md` и архив в `docs/archive/`.

**Фаза 7 — что именно закрыто:** `generateMetadata` (canonical/hreflang/OG) на всех страницах, `noindex` на отфильтрованных выдачах, генерация OG-картинок, JSON-LD (Product+AggregateOffer/BreadcrumbList/LocalBusiness), `sitemap.xml`/`robots.txt` из БД, GA4 (4 события) за cookie-баннером согласия. Lighthouse (Chromium из WSL, прод-сборка, mobile) — **perf 96/96/96, a11y 96/95/96, best-practices и SEO 100/100/100** на главной/категории/товаре, отчёты в `docs/perf/`. Один пункт **осознанно не тронут**: контраст бренд-строки на главной (`#9AA6B4` на белом) не проходит WCAG AA — но это буквальный хекс из `docs/WIREFRAMES.md` §5, не баг реализации; менять зафиксированный дизайн — решение владельца, не Claude Code.

**Фаза 8 — что именно закрыто:** 4 формальных E2E-сценария на `@playwright/test` (`tests/e2e/`) — фильтры с проверкой URL-состояния, переключение объёма с изменением цены, корзина с количеством и `reload`, сквозная заявка при `ORDERS_DRY_RUN=1` с проверкой записи в БД — плюс smoke на 3 типах страниц × 3 локали. Три прогона `pnpm test:e2e` подряд — 19/19 без единого флака (после фикса: параллельные воркеры `@playwright/test` создавали гонку в клиентской навигации на общем dev-сервере — исправлено `workers: 1` в `playwright.config.ts`, задокументировано в `docs/GOTCHAS.md`). Нагрузочный smoke — 120 синтетических товаров × 6 вариантов (`pnpm seed:load`, скрипт `scripts/seed-load.ts`) поверх демо-каталога, отклик листинга/фильтров/поиска/админки на 132 товарах/755 SKU — 5–312мс, без деградации (`docs/perf/full-load.md`); синтетические данные удалены после замера (`pnpm seed:load:clean`), каталог возвращён к 12 демо-товарам. **Ручная проверка (Приложение A, ★-пункты) НЕ пройдена** — только автоматика; сокращённая шпаргалка из 10 ★-пунктов передана владельцу в чате этой сессии, физически пройти её с телефона нужно после прода (см. «Ждёт владельца» — там же два пункта, которые в деве принципиально не проверить: боевая доставка в Telegram и https без mixed content).

**Открыто:** фаза 9 (инфраструктура продакшена) — идёт следующей, в основном на стороне владельца (домен, VPS, Coolify).

**Фаза 6 (дизайн-интеграция) — заморожена.** Текущий дизайн (главная, шапка, футер, эталонная карточка + рескин фазы 5) принят как есть; полный ПРОМПТ 6 с рескином остальных страниц НЕ запускается. Дальше по дизайну — только точечные правки, когда владелец обсудит с клиентом и составит мини-ТЗ; вносятся служебным промптом «Правки дизайнера» из `docs/PROMPTS.md` одним списком, не фазой 6 целиком.

**Важное отклонение от плана фазы 5 (зафиксировано в `PLAN.md` §5.2 и `CHANGELOG.md`):** `loading.tsx` сознательно НЕ добавлен — общий loading-индикатор сегмента ломает HTTP-статус `notFound()` ниже по дереву (стриминг Suspense отправляет `200` раньше, чем разрешится `404`; поймано регрессом `check-orders.mjs`, воспроизведено и в dev, и в прод-сборке, задокументировано в `docs/GOTCHAS.md`). Корректный статус 404 важнее общего лоадера при переходах.

## ЧТО ДАЛЬШЕ

### Шаг 0 — проверка перед стартом

```bash
cd ~/projects/parfume_site
git status   # .env НЕ коммитить — в нём реальный токен бота, должен быть в .gitignore
node -v && pnpm -v          # v22.x / 11.x
docker compose version
claude --version            # 2.1.x
head -20 docs/CHANGELOG.md  # последняя запись: фаза 8 (тестирование)
```

### Шаг 1 — фаза 9 «Инфраструктура продакшена»

Дать Claude Code **ПРОМПТ 9** из `docs/PROMPTS.md` (не переписывать, взять как есть). Основная часть — на стороне владельца (пункты 3–6 в «Ждёт владельца» ниже блокируют старт): домен, VPS, Coolify. `NEXT_PUBLIC_GA_ID` пока плейсхолдер — боевой Measurement ID заводится в этой фазе вместе с доменом. Код фазы 7 полностью рабочий и без него, канал просто не шлёт данные (проверено).

### Не забыть после деплоя на прод

**Ручная приёмка (Приложение A, ★-пункты) не пройдена руками — только автоматикой фазы 8.** Сокращённая шпаргалка из 10 пунктов с ожидаемым результатом на каждый уже передана в чате этой сессии (пункты 1–5, 7–9 можно пройти уже сейчас на деве по локальной сети; 2 пункта — заявка доходит в боевой Telegram, https без mixed content — принципиально требуют прод-домена и боевых каналов). После деплоя (конец фазы 9 / начало фазы 10) — пройти все 10 пунктов вживую с телефона на мобильном интернете (не по Wi-Fi), баги списком в чат.

### Дальше по плану

Фаза 9 → 10–11 (деплой, запуск + `ADMIN-GUIDE.md` по-русски, пишется последним; там же — ручная приёмка выше). Фаза 6 встраивается точечными правками, когда будет ТЗ по дизайну от клиента.

**Наполнение реальными товарами** снято с критического пути: у клиента пока нет ни товаров, ни данных 1С. Каталог/фильтры/карточка готовы с фазы 3, тесты гоняются на демо-данных, запуск возможен без реального прайса — он заливается лёгким CSV в любой момент, в т.ч. после прода.

## Ждёт владельца

| # | Что | Когда | Блокирует |
|---|---|---|---|
| 1 | **Мини-ТЗ по правкам дизайна** — обсудить с клиентом, собрать правки одним списком → служебный промпт «Правки дизайнера». Полный рескин остальных страниц (фаза 6) не делаем, дизайн принят как есть | когда удобно | точечные дизайн-правки (не блокирует остальные фазы) |
| 2 | **1С / товары** — у клиента пока нет ни товаров, ни артикулов 1С. Снято с критического пути: запуск возможен без реального прайса, каталог наполняется лёгким CSV в любой момент (импорт готов). Когда товары появятся — согласовать `sku` с учётом клиента, чтобы не пересопоставлять сотни позиций вручную. Экспорт заказа в 1С — позже, формат спросить у 1С-ника | когда у клиента появятся товары | ничего сейчас |
| 3 | **Прогнать письмо клиенту в песочнице Resend** — подставить тестовый ключ, отправить себе, проверить состав и ссылку. Код готов, живьём ни разу не отправлялся. 5 мин | до фазы 9 | нет |
| 4 | **Купить VPS + домен** — TopHost.md, тариф **VPS 3** (4 ядра / 8 ГБ / 75 ГБ NVMe): 1-й месяц 15.4 €, дальше 22 €/мес. Шаблон **Coolify-docker**, cPanel не брать. Домен `.md` ~25–30 €/год | к фазе 9 | фазы 9–10 |
| 5 | **Переслать `docs/translations-review.md`** на вычитку: RO — клиент/носитель, EN — ты или дизайнер. Отдельно на страницах Доставка/Возврат (`/admin/collections/pages`) проверить не только язык, а реальные условия (сроки, оплата, окно возврата 14 дней) — сейчас стартовый черновик | можно уже сейчас | вычитку переводов (фаза 11) |
| 6 | **Обновить чек-лист клиента** (v3): этап 4 закрыт, этап 5 (акции = фаза 4.5) закрыт, поток заявки и отслеживание (4.6/4.7) закрыты, контент-страницы и бренды (фаза 5) закрыты, SEO и производительность (фаза 7) закрыты, тестирование (фаза 8) закрыто | не срочно | нет |
| 7 | **Ручная приёмка (Приложение A, ★-шпаргалка из 10 пунктов)** — фаза 8 закрыла только автоматику (Playwright), руками на реальном телефоне не проверялось. Шпаргалка с ожидаемым результатом на каждый пункт передана в чате этой сессии (2026-08-12) — сохрани её себе, тут не дублируется. 8 из 10 пунктов можно пройти уже сейчас на деве по локальной сети, 2 (боевая доставка в Telegram, https без mixed content) — только после прод-домена | после деплоя (фаза 9→10), на мобильном интернете, не по Wi-Fi | сдачу фазы 10 |
| 8 | **Линт-долг фазы 7** — `pnpm lint` даёт 9 ошибок + 2 предупреждения в файлах фазы 7 (`NewOrdersCard.tsx`, `ConsentBanner.tsx`, `GoogleAnalytics.tsx`, `JsonLd.tsx`). Тесты и типы чисты, но прод-сборка Next может падать на ESLint. Почини до деплоя одним фикс-промптом («Прочитай CLAUDE.md и docs/CHANGELOG.md. Проблема: `pnpm lint` — 9 ошибок/2 предупреждения в файлах фазы 7 … Исправь, ритуал») | до фазы 10 (перед `pnpm build` на проде) | сдачу фазы 10 |

## Организационный контекст

- **Хранилище фото:** на старте диск VPS, S3 — потом (переключается адаптером Payload).
- **Email заявок:** Resend, ключ пока плейсхолдер `CHANGEME_RESEND`. Письмо менеджеру (фаза 4) и клиенту (4.6) написаны, включатся боевыми, когда появится домен и верифицированный отправитель.
- **Telegram:** работает боевым, проверено вживую. Бот `@mooflaco_order_bot`, `TELEGRAM_CHAT_ID=799511005`, токен в `.env`. `ORDERS_DRY_RUN=1` — осознанный дефолт в деве, гасит внешние отправки при самопроверках.
- **Регистрация / личные кабинеты:** не нужны, только гостевой заказ. Отслеживание — по токену или номер+телефон, без ЛК.
- **Оплата:** онлайн нет. Позже — Paynet или эквайринг maib / Victoriabank. Stripe в Молдове недоступен.
- **Документы клиенту** (у владельца, не в репо): презентация v7 (docx + pdf), чек-лист v3 (docx + pdf). Цена проекта — **1 200 €** единоразово, поддержка 2 месяца включена.

## Карта документации

| Файл | Отвечает на вопрос | Кто пишет |
|---|---|---|
| `CLAUDE.md` | как работать: правила, запреты, стек, ритуал | владелец |
| `docs/STATE.md` | **где мы и что дальше** (этот файл) | владелец / чат-штаб |
| `docs/GOTCHAS.md` | грабли и найденные решения | Claude Code дописывает |
| `docs/CHANGELOG.md` | что сделано, по датам (+ `docs/archive/`) | Claude Code, ритуал завершения |
| `docs/PLAN.md` | спецификация фаз и критерии приёмки | владелец |
| `docs/PROMPTS.md` | готовые промпты по фазам | владелец |
| `docs/BRAND.md`, `docs/WIREFRAMES.md`, `docs/mockups/` | дизайн-правда | владелец |
| `docs/import-guide.md`, `docs/design-brief.md`, `docs/client-questions.md` | операционка | владелец |

## Как обновлять этот файл

В конце сессии, если закрыта фаза/подфаза, изменился список «Ждёт владельца», сместился план или появился новый блокер:

1. Правится **только раздел «Где мы», «Что дальше» и «Ждёт владельца»** — остальное статично.
2. Дата в шапке — обязательно.
3. Файл сохраняется в репозиторий **и** перезаливается в файлы проекта claude.ai (одна и та же версия в двух местах, имя не меняется — никаких `-4`, `-5`).
