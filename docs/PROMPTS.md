# PROMPTS.md — плейбук промптов для Claude Code

Рабочая последовательность проекта. Каждый блок в рамке — **готовый промпт**: копируешь целиком в Claude Code. Всё остальное он делает и проверяет сам (правила — в `CLAUDE.md`), ведёт `docs/CHANGELOG.md` и в конце выдаёт **⎇ git-блок** — ты его вставляешь в терминал, это и есть твой git.

**Твоя работа на весь проект:** вставлять промпты → вставлять git-блоки → заполнять короткие клик-листы (аккаунты/оплаты, которые физически нельзя автоматизировать) → изредка смотреть глазами (👁).

Файлы в репозитории: `CLAUDE.md` — корень; `PLAN.md`, `PROMPTS.md` — в `docs/`.

---

## ШАГ 0 — Бутстрап руками (единственный ручной блок, ~20 минут, один раз)

До первого промпта Claude Code должен существовать. Только это делается вручную.

**Docker — нативно внутри Ubuntu (без Docker Desktop):**
```bash
# 1. Включить systemd, чтобы демон Docker стартовал сам
sudo tee /etc/wsl.conf > /dev/null << 'EOF'
[boot]
systemd=true
EOF
```
Затем в PowerShell: `wsl --shutdown`, открыть Ubuntu заново и продолжить:
```bash
# 2. docker-ce + compose-плагин по официальной инструкции docs.docker.com/engine/install/ubuntu
#    (блок из ~6 команд: репозиторий Docker + apt install docker-ce docker-compose-plugin)
# 3. Работа без sudo:
sudo usermod -aG docker $USER && newgrp docker
# 4. Проверка:
docker run hello-world && docker compose version
```

**В WSL Ubuntu (инструменты):**
```bash
# nvm — одной curl-командой из README github.com/nvm-sh/nvm, затем:
nvm install 22 && corepack enable
git config --global core.autocrlf input
git config --global init.defaultBranch main
mkdir -p ~/projects/parfum && cd ~/projects/parfum && git init
```

**Claude Code:** установка внутри WSL по официальной инструкции https://code.claude.com/docs/en/setup (нативный установщик; альтернатива — `npm install -g @anthropic-ai/claude-code`). Затем `claude` → логин (план Pro/Max или ключ Console) → `claude doctor` — всё зелёное.

**GitHub:** создать приватный репозиторий в браузере (пустой), привязать:
```bash
ssh-keygen -t ed25519    # если ключа нет; .pub добавить в GitHub → Settings → SSH keys
git remote add origin git@github.com:ТВОЙ_ЛОГИН/ИМЯ_РЕПО.git
```

Положить в папку проекта три файла: `CLAUDE.md` (корень), `docs/PLAN.md`, `docs/PROMPTS.md`. Готово — дальше только промпты.

> Если `next build` когда-нибудь упадёт по памяти (OOM): WSL по умолчанию берёт 50% RAM. Лечится файлом `C:\Users\<user>\.wslconfig` → `[wsl2]` `memory=10GB`, `swap=8GB` + `wsl --shutdown`. До проблемы — не трогать.

---

## ПРОМПТ 1 — Инициализация проекта (фаза 1 + хвосты фазы 0)

```
Прочитай CLAUDE.md, docs/PLAN.md и docs/BRAND.md. Создай docs/CHANGELOG.md и выполни фазу 1 целиком (шаги 1.1–1.5): каркас create-payload-app (blank, postgres, typescript) с нашей структурой папок; docker-compose.dev.yml и подними БД; локальный .env со сгенерированными секретами (внешние ключи — плейсхолдеры CHANGEME_*); каркас i18n ro/ru/en с префиксом всегда; tailwind v4 + shadcn/ui + src/styles/tokens.css с брендовыми токенами из BRAND.md §8 и шрифтом Inter через next/font (subsets latin+latin-ext+cyrillic — румынские диакритики обязательны); прод-Dockerfile (standalone) с проверкой сборки и запуска образа. Дополнительно: сгенерируй docs/design-brief.md — задание дизайнеру на макеты/вайрфреймы страниц по Приложению B плана с опорой на утверждённый брендбук (BRAND.md), включив запрос SVG-исходников из BRAND.md §9; и docs/client-questions.md по шагу 0.6. Все самопроверки шагов выполни сам (dev-сервер, /admin с первым админом admin@local.dev, три локали, docker build). Заверши ритуалом из CLAUDE.md.
```

**Твоё до:** ничего. **Твоё после:** вставить ⎇ git-блок; отправить дизайнеру `docs/design-brief.md`, клиенту — `docs/client-questions.md` (📤 критический путь — в тот же день).

---

## ПРОМПТ 2 — Модель данных и импорт (фаза 2)

```
Прочитай CLAUDE.md, docs/PLAN.md и docs/CHANGELOG.md. Выполни фазу 2 целиком: все коллекции и глобалы по спецификации (локализация полей, хуки денормализации minPrice/maxPrice/inStock и revalidateTag, роли admin/manager, индексы), payload generate:types; seed-скрипт с 10 товарами на трёх локалях; CSV-импорт по ТЗ §4.7 — движок в src/lib/import + CLI (форматы A и B, upsert по handle/sku, связи по slug с auto-create, Zod-валидация с номерами строк, транзакция всё-или-ничего, --dry-run), лёгкий CSV sku,price,stock и CSV переводов; шаблон для клиента docs/import-template.csv с примером заполнения. Самопроверка: сгенерируй тестовый CSV сам, прогони dry-run на битом файле, дважды импортируй валидный (проверь отсутствие дублей), лёгким CSV измени цены, tsc чист. Заверши ритуалом.
```

**Твоё до:** ничего. **Твоё после:** git-блок; отправить клиенту `docs/import-template.csv`.

---

## ПРОМПТ 3 — Каталог (фаза 3)

```
Прочитай CLAUDE.md, docs/PLAN.md, docs/BRAND.md, docs/WIREFRAMES.md и docs/CHANGELOG.md. Выполни фазу 3 целиком: layout строго по WIREFRAMES.md и мокапу docs/mockups/home.html (шапка navy со знаком, поиском, переключателем локали и корзиной-бейджем; sticky-сжатие до знака; футер 4 колонки; мобильное меню; breadcrumbs); листинг каталога и категорий с пагинацией по 24 и unstable_cache с тегами — карточка товара по эталону из WIREFRAMES.md §3; фасетные фильтры на nuqs (бренд мультивыбор с поиском, цена range, объём, пол, ноты, флаги, счётчики фасетов, сброс, мобильный bottom-sheet) и сортировки; карточка товара с галереей, переключателем объёма без перезагрузки, пирамидой нот, похожими товарами, кнопками корзины; FTS-поиск с конфигами romanian/russian/english и автодополнением. Самопроверку каждого шага выполняй сам, включая скриншоты Playwright на 360/768/1280 со сверкой против мокапа и проверку, что ссылка с фильтрами воспроизводит состояние. Атомарные коммиты накапливай, один git-блок в конце фазы. Заверши ритуалом.
```

**Твоё до:** ничего. **Твоё после:** git-блок; 👁 открыть каталог с телефона (адрес в локальной сети даст CC) — 5 минут. 🔶 Это точка демо клиенту.

---

## ПРОМПТ 4 — Корзина и заявки (фаза 4)

```
Прочитай CLAUDE.md, docs/PLAN.md и docs/CHANGELOG.md. Выполни фазу 4. Сначала выдай мне инструкцию на 5 строк: создать тестового бота в BotFather и узнать свой chat_id — я пришлю токен и id, впиши их в .env сам и продолжай. Дальше: корзина Zustand persist с мини-корзиной; форма заявки (имя, телефон с маской +373, мессенджер, комментарий); route handler — Zod, создание order через Local API, параллельно Telegram sendMessage и письмо Resend (Resend пока плейсхолдер), honeypot + rate-limit по IP, флаг ORDERS_DRY_RUN; страница спасибо с событием generate_lead; рабочее место менеджера в админке (колонки, статусы, ограничение прав). Самопроверка: сквозная заявка при DRY_RUN с проверкой записи в БД, одна реальная отправка в мой тест-бот, отказ невалидного телефона и honeypot. Заверши ритуалом.
```

**Твоё до:** 3 минуты — бот в BotFather по инструкции CC, прислать токен и chat_id. **Твоё после:** git-блок; 👁 убедиться, что сообщение пришло в Telegram.

---

## ПРОМПТ 4.5 — Акции и промо (фаза 4.5)

```
Прочитай CLAUDE.md, docs/PLAN.md, docs/BRAND.md, docs/WIREFRAMES.md и docs/CHANGELOG.md. Выполни фазу 4.5. Схему вариантов не менять — используй уже существующее поле oldPrice, ручного тега «Sale»/isSale в модели нет и не заводи (единственный источник скидочности — денормализованный hasDiscount, в UI подписан как Sale). Хук денормализации products (тот же, что считает minPrice/maxPrice/inStock) дополни hasDiscount и maxDiscountPercent. Бейдж скидки «−N%» (navy/cream, только токены) в правом верхнем углу фото-зоны эталонной карточки + зачёркнутая старая цена рядом с ценой — на карточке (во всех сетках: каталог/поиск/похожие) и на странице товара с пересчётом при смене объёма. Вариант B: бейдж и цена карточки всегда об одном и том же варианте — если есть уценённые варианты, выбирается тот с максимальным % скидки и показывается целиком (его цена как «от N MDL», даже не с минимального объёма, его же зачёркнутая oldPrice, его бейдж); без уценённых вариантов — обычная минимальная цена без бейджа. Инвариант: бейдж без зачёркнутой цены и наоборот — невозможны. Фасет «со скидкой» по hasDiscount со счётчиком (в UI — Sale) и сортировка «по размеру скидки» — по аналогии с прочими флагами/сортировками. Промо-баннер в global settings (enabled, локализованный текст, startDate/endDate) — узкая полоса над шапкой на всех страницах по датам, cream/navy, закрытие на sessionStorage, не ломает sticky-шапку; ссылка — select фиксированных внутренних целей (Главная/Каталог/Каталог со скидками/Бренды/О нас/Доставка/Контакты) + необязательный override для внешних URL, апгрейд до Pages-relationship — в фазе 5. Акционный hero в global homepage (promoHero: enabled, локализованные заголовок/подстрока/CTA, опциональный фон, startDate/endDate, тот же select+override для CTA-ссылки) — целиком заменяет обычный hero на заданный период, та же композиция/типографика §1, без слайдера/ротации, server-rendered. Все новые строки — во все три messages/*.json. Самопроверка: в seed — товар с уценкой не на самом дешёвом объёме (для варианта B), isSale из сида убран; проверь, что фасета «Sale» ровно одна (авто) со честным счётчиком, инвариант бейдж⇔зачёркивание держится в каталоге/поиске/похожих, select ссылок баннера/hero даёт правильный локализованный URL и override его перебивает, sticky-шапку с баннером, регресс check-catalog/check-mockup/check-stock/check-sheet, tsc чист. Заверши ритуалом.
```

**Твоё до:** ничего. **Твоё после:** git-блок; 👁 включить баннер/акционный hero на 5 минут в `/admin` и глянуть глазами на реальных датах перед показом клиенту.

---

## ПРОМПТ 4.6 — Поток заявки (фаза 4.6)

```
Прочитай CLAUDE.md, docs/PLAN.md и docs/CHANGELOG.md. Выполни фазу 4.6. Форма заявки получает необязательное поле email (Zod: если заполнено — валидный email, не просто непустая строка; подсказка «для подтверждения заказа и ссылки на его статус»), телефон остаётся единственным обязательным контактным каналом. orders.customer получает необязательное поле email — миграцией (push:false). Письмо менеджеру (src/lib/orders/email.ts::sendEmail, фаза 4.2) уже реализовано — не переписывай его; добавь ВТОРОЕ, отдельное письмо клиенту: состав заказа, сумма, ссылка на страницу статуса заказа (/order/<токен> — сама страница появится в фазе 4.7, здесь только ссылка и, если поля ещё нет, минимальное поле orders.statusToken под неё), локализовано по order.locale, уходит только если у заявки есть customer.email — иначе тихо пропускается. Тот же ORDERS_DRY_RUN глушит оба письма разом, то же правило «плейсхолдер-ключ — канал пропущен без падения». Явно запиши в PLAN.md/CHANGELOG зависимость: боевая доставка обоих писем требует в Resend подтверждённого домена и верифицированного отправителя (RESEND_API_KEY сейчас CHANGEME_RESEND, домен — фаза 9); для локальной проверки прямо сейчас используй режим песочницы Resend (тестовый ключ, отправитель onboarding@resend.dev — уже дефолт в коде). Страница «Спасибо» — расширь блоком деталей заказа под благодарностью: номер, состав с объёмами и количеством, сумма, контакты клиента, ссылка на страницу статуса (ссылка может 404-ить до фазы 4.7 — это ожидаемо, отметь явно). Почини баг навигации: пункт «Новинки» в шапке сейчас ведёт на /catalog?new=1, который каталог не понимает (нужен flags=isNew, как flags=hasDiscount у скидок) — добавь цель catalogNew в LINK_TARGETS (src/lib/links.ts) и переведи navigation.header[].href на тот же select+override (internalLinkFields), что уже есть у промо-баннера/hero; пункт «Бренды» получает уже существующую цель brands (ведёт в 404 до фазы 5 — ожидаемо). Все новые строки — во все три messages/*.json. Самопроверка: заявка без email отправляется как раньше, с невалидным email отклоняется; заявка с email реально получает оба письма в песочнице Resend (проверь боевую отправку, не только dry-run); детали на thank-you совпадают с тем, что ушло в Telegram/письмо; клик по «Новинки» в шапке реально фильтрует каталог (flags=isNew в URL); tsc чист, регресс check-catalog/check-orders/check-promo. Заверши ритуалом.
```

**Твоё до:** ничего. **Твоё после:** git-блок; 👁 если под рукой тестовый Resend-аккаунт — глянуть, что письмо клиенту реально пришло в песочнице (необязательно, можно отложить до фазы 9, где появится боевой домен).

---

## ПРОМПТ 4.7 — Отслеживание заказа и админка (фаза 4.7)

```
Прочитай CLAUDE.md, docs/PLAN.md и docs/CHANGELOG.md. Выполни фазу 4.7. Поле status у orders уже есть (фаза 4.3) — новое не заводи; приведи набор значений к new/confirmed/ready/issued/cancelled (новая/подтверждена/готова/выдана/отменена, дефолт «новая») — это Postgres ENUM, смена набора и переименование системных значений (contacted→confirmed, done→issued) идёт ОДНОЙ миграцией (push:false) с переносом данных уже существующих заявок, ни одна не должна остаться на невалидном значении; подписи статуса локализуй во все три messages/*.json (нужны публичной странице), admin-подписи (ORDER_STATUSES) остаются хардкод-русскими, как весь остальной admin UI. Публичная страница статуса заказа без регистрации: при создании заказа генерируется случайный неугадываемый токен (новое поле orders.statusToken, уникальное, индексированное, только для чтения) — если поле уже заведено в фазе 4.6, доделай отсюда генерацию и саму страницу /[locale]/order/[token]; маршрут не должен конфликтовать ни с одним /api/<slug-коллекции> REST Payload. Запасной путь — форма поиска по номеру заказа И телефону вместе, с тем же rate-limit по IP, что у формы заявки (по образцу checkRateLimit/clientIp, фаза 4.2). ЯВНО зафиксируй и в PLAN.md, и в коде: поиск по одному лишь номеру заказа запрещён на уровне API, не только скрыт в UI — иначе перебором читаются чужие персональные данные. Страница показывает состав/статус/сумму человеческим языком на трёх локалях, без единого служебного поля. На дашборде админки — карточка «Новых заявок: N» со ссылкой на список, отфильтрованный по status=new, и цветная плашка статуса в списке заказов — цвета только через переменные tokens.css. Кнопка скачивания CSV — не только в карточке заявки (уже есть), но и в строке списка рядом с номером; массовое действие — выделить несколько заявок и получить один общий CSV тем же форматом (UTF-8 BOM, разделитель «;»). Почини админку: переключение языка ro/ru/en (проверь supportedLanguages в payload.config.ts и язык профиля пользователя — сейчас работает только русский), верни кнопку выхода из профиля, если её перекрыл кастомный layout, добавь «Назад» и переход на Dashboard в кастомных экранах; дефолтный язык интерфейса для нового пользователя — русский. Брендинг админки — логотип MON FLACON, акценты navy/cream, читаемые заголовки, только через переменные tokens.css, без переверстки структуры Payload admin. Самопроверка: пять статусов и корректный перенос старых значений после миграции, токен практически не подбираем, запрос без телефона (только номер) отклоняется на уровне API, rate-limit формы поиска работает, счётчик новых заявок честный, кнопка CSV в списке и массовый экспорт дают корректный файл, переключение языка /admin реально меняет интерфейс на всех трёх, кнопка выхода видна, tsc чист, регресс check-orders/check-catalog. Заверши ритуалом.
```

**Твоё до:** ничего. **Твоё после:** git-блок; 👁 открыть публичную страницу статуса по ссылке из тестовой заявки и через запасной поиск номер+телефон, полистать `/admin` на всех трёх языках — 10 минут.

---

## ПРОМПТ 5 — Контент и страницы (фаза 5)

```
Прочитай CLAUDE.md, docs/PLAN.md, docs/BRAND.md, docs/WIREFRAMES.md и docs/CHANGELOG.md. Выполни фазу 5: главная строго по WIREFRAMES.md §Главная и мокапу docs/mockups/home.html — hero без слайдера, лента категорий с живыми счётчиками, новинки по флагу isNew, editorial-блок по формуле 1 image + 1 phrase + logo, бренд-строка, опциональный слот «Хиты»; весь контент управляется из global homepage и локализован; финальную вёрстку сверь скриншотом с мокапом на 360/768/1280. Далее: новая коллекция Pages (localized title/body/seo, slug) — на ней строятся статические страницы О нас/Доставка/Возврат/Контакты (это закрывает 404 у одноимённых целей LINK_TARGETS из фазы 4.5.4/4.5.5/4.6.4 — до этого шага ожидаемый); /brands с указателем и страницы брендов с их товарами (закрывает цель brands); апгрейд select-полей linkTarget/ctaTarget промо-баннера и акционного hero (и, если уже применено в навигации, — тоже) до relationship на Pages, с обязательной отдельной веткой для не-Pages целей (Каталог/Каталог со скидками/Каталог с новинками/Главная) и сохранением уже введённого override для внешних URL — ранее сохранённые строковые значения должны замаппиться на Pages-документы миграцией, не потеряться; кастомные 404/error/loading по всем разделам; полная ревизия messages/*.json на три локали с пометками TODO review на машинных переводах и списком для вычитки в docs/translations-review.md. Самопроверка: обход всех страниц на трёх локалях без missing keys и console errors, правка контента из админки отражается на витрине, все ранее-404 цели теперь открывают реальный контент, старые значения select-полей не потерялись при апгрейде на relationship. Заверши ритуалом.
```

**Твоё до:** ничего. **Твоё после:** git-блок; переслать `docs/translations-review.md` на вычитку (клиент — RO, ты/дизайнер — EN).

---

## ПРОМПТ 6 — Интеграция дизайна (фаза 6, когда макеты готовы)

```
Прочитай CLAUDE.md, docs/PLAN.md, docs/BRAND.md, docs/WIREFRAMES.md и docs/CHANGELOG.md. Выполни фазу 6. Главная, шапка, футер и эталонная карточка товара уже утверждены (WIREFRAMES.md + docs/mockups/home.html) — их не переделывай. Вход: [вайрфреймы остальных страниц: каталог с фильтрами, карточка товара, корзина/заявка, страница бренда, статика — приложу; если дизайнер дал финальные макеты — тоже здесь]. Задача: дополни WIREFRAMES.md новыми секциями по входу, приведи соответствующие страницы к вайрфреймам и издательскому языку бренда (BRAND.md §5) — распределение navy-«рамы» и светлых зон, паттерн, воздух; перенеси паттерны главной на новые страницы. Подключи SVG лого/знака/паттерна от дизайнера, как только приложу (до того — текущие плейсхолдеры той же геометрии). После каждой страницы — скриншот-сверка Playwright на 360/768/1280, расхождения и вопросы — в docs/design-diff.md. Атомарные коммиты, один git-блок в конце. Заверши ритуалом.
```

**Твоё до:** приложить структуру/вайрфреймы (+ SVG-исходники от дизайнера, когда будут). **Твоё после:** git-блок; показать дизайнеру и клиенту, правки собрать одним списком → промпт-фикс (шаблон в конце файла).

---

## ПРОМПТ 7 — SEO и производительность (фаза 7)

```
Прочитай CLAUDE.md, docs/PLAN.md и docs/CHANGELOG.md. Выполни фазу 7: generateMetadata везде с localized title/description, canonical и alternates.languages (ro/ru/en + x-default); noindex на выдачах с активными фильтрами; OG-теги и генерация OG-изображений; JSON-LD Product+AggregateOffer/BreadcrumbList/Organization; sitemap.ts из БД со всеми локалями и alternates; robots.ts; аудит производительности — next/image sizes/priority/blur, динамические импорты, шрифты; GA4 события view_item/add_to_cart/begin_checkout/generate_lead с загрузкой после согласия и простым cookie-баннером (NEXT_PUBLIC_GA_ID — плейсхолдер). Lighthouse прогоняй сам через CLI на chromium из Playwright для главной/категории/товара mobile, отчёты в docs/perf/, фиксь до perf ≥ 90 и SEO/a11y ≥ 95. Самопроверка разметки: валидация JSON-LD и hreflang, curl sitemap/robots. Заверши ритуалом.
```

**Твоё до:** ничего. **Твоё после:** git-блок.

---

## ПРОМПТ 8 — Тестирование (фаза 8)

```
Прочитай CLAUDE.md, docs/PLAN.md и docs/CHANGELOG.md. Выполни фазу 8: установи Playwright с системными зависимостями (sudo разрешён); напиши 4 сценария — фильтры с проверкой URL-состояния, переключение объёма с изменением цены, корзина с количеством и reload, сквозная заявка при ORDERS_DRY_RUN с проверкой записи в БД; плюс smoke трёх типов страниц на трёх локалях без console errors. Добейся стабильности: три прогона подряд без флаков. Затем нагрузочный smoke: сгенерируй 120 товаров по 6 вариантов (или импортируй реальный прайс, если файл уже в docs/), замерь отклик листинга/фильтров/поиска и работу админки, результаты в docs/perf/full-load.md. В конце выдай мне шпаргалку из ★-пунктов Приложения A для проверки с телефона — коротким списком с ожидаемым результатом на пункт. Заверши ритуалом.
```

**Твоё до:** если реальный прайс клиента уже есть — положить в `docs/`. **Твоё после:** git-блок; 👁 пройти ★-шпаргалку с телефона (~15 минут). 🔶 Локальная версия готова к проду.

---

## ПРОМПТ 9 — Инфраструктура (фаза 9)

```
Прочитай CLAUDE.md, docs/PLAN.md и docs/CHANGELOG.md. Выполни фазу 9 в режиме максимальной подготовки за меня. Сгенерируй: (1) docs/go-live-checklist.md — клик-лист регистраций строго по порядку с полями "результат впиши сюда": домен (варианты зон и где брать .md), Cloudflare add site + смена NS + создание R2 bucket и S3-ключей + SSL mode Full strict при сером облаке, Hetzner CX22 Ubuntu 24.04 Falkenstein с моим ssh-ключом (команду генерации дай), боевой Telegram-бот и chat_id, Resend с DNS-записями для Cloudflare, GA4-поток; (2) server/setup.sh — весь харднинг одним идемпотентным скриптом (deploy-юзер, ssh только по ключу, ufw 22/80/443+8000, fail2ban, unattended-upgrades, swap 2G) и server/README.md с ровно двумя командами для его запуска с моей машины; (3) инструкцию установки Coolify и настройки: подключение GitHub, ресурс Postgres 17 без публикации наружу, S3-бэкапы в R2 ежедневно с хранением 30 дней. SSH сам не выполняй, жди от меня заполненный клик-лист. Заверши ритуалом.
```

**Твоё до:** ничего. **Твоё после:** git-блок; пройти клик-лист (~40 минут кликов и оплат), выполнить две команды из `server/README.md`, вернуть CC заполненный клик-лист. Хочешь совсем без ручных ssh — раскомментируй OPT-IN строку в CLAUDE.md, и CC выполнит `server/setup.sh` сам.

---

## ПРОМПТ 10 — Деплой (фаза 10)

```
Прочитай CLAUDE.md, docs/PLAN.md, docs/CHANGELOG.md и заполненный docs/go-live-checklist.md. Выполни фазу 10: (1) сгенерируй docs/deploy-coolify.md — точная пошаговка панели: приложение из репо ветка main, Build Pack Dockerfile, полный список prod-env (PAYLOAD_SECRET сгенерируй и покажи один раз с пометкой сохранить, внешние ключи — из клик-листа, ORDERS_DRY_RUN=0), домены apex+www; (2) порядок DNS: A-записи при сером облаке → дождаться Let's Encrypt → включить proxy → редирект www; (3) после моего сообщения "задеплоено" — прогони прод-smoke сам: curl главной на трёх локалях, товар, sitemap, robots, редирект www, https без mixed content, security headers — отчёт таблицей; (4) инструкция первичного импорта каталога на прод (terminal контейнера в Coolify или кнопка админки) и создания админа+менеджера; (5) git-блок перехода на рабочую схему: ветка dev, main = прод, тег v1.0.0. Заверши ритуалом.
```

**Твоё до:** заполненный клик-лист из П9. **Твоё после:** прокликать `docs/deploy-coolify.md` (~20 минут), написать CC «задеплоено», вставить git-блок. 🔶 Прод жив.

---

## ПРОМПТ 11 — Запуск и передача (фаза 11)

```
Прочитай CLAUDE.md, docs/PLAN.md и docs/CHANGELOG.md. Выполни фазу 11: (1) docs/ADMIN-GUIDE.md на русском для клиента — пиши его ПОСЛЕДНИМ пунктом, после всех остальных правок этой фазы и уже после того, как вся админка (включая фазу 4.7) не будет больше меняться, иначе гайд устареет раньше, чем попадёт к клиенту: приём и обработка заявки (включая страницу статуса заказа), смена статуса заявки (5 значений, что каждое означает), экспорт CSV (одиночный и массовый), редактирование товаров и цен/остатков лёгким CSV, редактирование промо-блоков (баннер, акционный hero, ссылки на страницы), добавление товара, добавление перевода, мини-FAQ — со скриншотами русского интерфейса админки (дефолтный язык клиента); (2) docs/restore-drill.md — проверка бэкапа восстановлением: скачать дамп из R2, развернуть в локальный Postgres, команды готовыми блоками; (3) docs/launch-checklist.md — клик-лист финала: Search Console с sitemap и проверкой hreflang, UptimeRobot, Sentry prod DSN, вычитка переводов; (4) финальная запись в changelog — итог запуска: что в проде, версия, известные отложенные пункты. Заверши ритуалом.
```

**Твоё до:** ничего. **Твоё после:** git-блок; пройти `launch-checklist` и `restore-drill` (restore — обязательно, бэкап без проверки восстановлением — не бэкап); передать клиенту доступы + `ADMIN-GUIDE.md`, демо-созвон. 🚀

---

## Служебные промпты

**Продолжение (каждая новая сессия):**
```
Прочитай CLAUDE.md, docs/PLAN.md и docs/CHANGELOG.md. Определи по changelog, где мы остановились, подтверди статус одной строкой и продолжай с первого незакрытого пункта.
```

**Фикс бага / правки:**
```
Прочитай CLAUDE.md и docs/CHANGELOG.md. Проблема: [описание, шаги воспроизведения, скрин если есть]. Найди причину, исправь, добавь защиту от регресса (тест, если уместно), запись в changelog с типом fix, заверши ритуалом.
```

**Правки дизайнера (после П6):**
```
Прочитай CLAUDE.md и docs/design-diff.md. Правки дизайнера одним списком: [список]. Внеси все, обнови скриншот-сверку, запись в changelog, заверши ритуалом.
```

---

## Карта участия владельца (всё, что на тебе, целиком)

| Когда | Что | Время |
|---|---|---|
| Шаг 0 | бутстрап окружения, один раз | ~20 мин |
| После П1 | переслать бриф дизайнеру и вопросы клиенту | 5 мин |
| П4 | тест-бот в BotFather | 3 мин |
| После каждого промпта | вставить ⎇ git-блок | 30 сек |
| После П3, П4, П8 | 👁 взгляд с телефона | 5–15 мин |
| После П4.6 (необязательно) | 👁 проверить письмо-подтверждение в песочнице Resend | 5 мин |
| После П4.7 | 👁 страница статуса заказа + языки `/admin` | 10 мин |
| П9 | клик-лист аккаунтов + 2 команды на сервер | ~40 мин |
| П10 | прокликать деплой по готовой пошаговке | ~20 мин |
| П11 | launch-чеклист + restore-drill + передача | ~40 мин |

Всё остальное — Claude Code, включая проверки.
